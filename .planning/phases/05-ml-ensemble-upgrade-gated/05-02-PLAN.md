---
phase: 05-ml-ensemble-upgrade-gated
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/cdd_mundial/models/ml_xgboost.py
  - src/cdd_mundial/models/ml_validation.py
  - src/cdd_mundial/models/__init__.py
  - tests/test_ml_validation.py
autonomous: true
requirements:
  - ML-02
decision_ids:
  - D-04
  - D-05
  - D-10
must_haves:
  truths:
    - "El primer clasificador ML debe ser pequeno, auditable y entrenado con la misma validacion temporal estricta que el baseline."
    - "El objetivo es multiclass probabilistico 3-way, no prediccion de score ni tuning amplio de leaderboard."
    - "Las filas no elegibles para ML por cobertura insuficiente no contaminan el ajuste."
  artifacts:
    - path: "src/cdd_mundial/models/ml_xgboost.py"
      provides: "Fit/predict conservador de XGBoost multiclass con semillas y contratos deterministas"
    - path: "src/cdd_mundial/models/ml_validation.py"
      provides: "Harness temporal ML sobre los cuatro holdouts con artefactos fechados"
    - path: "tests/test_ml_validation.py"
      provides: "Pruebas del protocolo temporal, exclusion de filas inelegibles y forma/probabilidad del predictor"
  key_links:
    - from: "src/cdd_mundial/models/ml_validation.py"
      to: "src/cdd_mundial/models/validation.py"
      via: "reuso del mismo calendario de holdouts y metricas log-loss/Brier/RPS"
      pattern: "HOLDOUTS|select_holdout|log_loss"
    - from: "src/cdd_mundial/models/ml_validation.py"
      to: "src/cdd_mundial/models/ml_features.py"
      via: "dataset point-in-time canonico y metadata de elegibilidad"
      pattern: "eligible|feature_columns"
---

<objective>
Implementar el clasificador XGBoost multiclass y su validacion temporal estricta.

Purpose: cerrar `ML-02` sin inventar un pipeline paralelo al baseline.
Output: modelo multiclass conservador, harness de holdouts fechado, y artefactos de prediccion/evaluacion reproducibles.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-01-PLAN.md
@src/cdd_mundial/models/validation.py
@src/cdd_mundial/models/baselines.py
@tests/test_validation_temporal.py
@tests/test_baselines.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement a conservative multiclass XGBoost wrapper</name>
  <read_first>
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
    - src/cdd_mundial/models/baselines.py
    - tests/test_baselines.py
  </read_first>
  <files>src/cdd_mundial/models/ml_xgboost.py, src/cdd_mundial/models/__init__.py, tests/test_ml_validation.py</files>
  <action>Add a small public wrapper around XGBoost for 3-class probability prediction. Keep the first-pass model shallow and deterministic, with explicit seeds, auditable defaults, and no broad hyperparameter search. Write RED tests first for valid probability rows, deterministic repeated predictions under fixed seeds, and clear rejection of malformed inputs.</action>
  <acceptance_criteria>The project can fit and predict a multiclass probability model with a stable API and sane constrained defaults aligned with D-05.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_validation.py -k xgboost -x</automated>
  </verify>
  <done>The repo contains a reusable XGBoost predictor module rather than embedding training logic in notebooks or ad hoc scripts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the Phase 2 holdout harness for ML-only evaluation</name>
  <read_first>
    - src/cdd_mundial/models/validation.py
    - tests/test_validation_temporal.py
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-01-PLAN.md
  </read_first>
  <files>src/cdd_mundial/models/ml_validation.py, tests/test_ml_validation.py</files>
  <action>Create a dated validation harness for the ML-only candidate that reuses the exact four holdouts and metric family from Phase 2. The harness must build features strictly before each holdout cutoff, exclude ineligible rows from ML fitting/scoring as required by D-04, and persist reviewable artifacts parallel to the baseline style: report JSON plus holdout prediction table.</action>
  <acceptance_criteria>The ML candidate can be evaluated across `wc2018`, `wc2022`, `euro2024`, and `copa2024` under the same fit-at-cutoff semantics as the baseline, with artifacts that explain what was trained, scored, and excluded.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_validation.py -x</automated>
  </verify>
  <done>`ML-02` is backed by the same temporal rigor as `MODEL-04`, making later candidate comparisons legitimate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ML feature table -> XGBoost fit | Canonical supervised rows become model parameters |
| temporal folds -> validation report | Holdout evidence becomes a phase-level claim about ML value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | holdout evaluation | mitigate | Reuse the same four holdouts and fit-at-cutoff pattern from Phase 2 verbatim. |
| T-05-05 | R | model determinism | mitigate | Fix seeds and test repeatability for wrapper-level predictions and persisted artifacts. |
| T-05-06 | T | low-coverage training rows | mitigate | Exclude ineligible rows via the metadata emitted by `ml_features.py` and assert the exclusion path in tests. |
</threat_model>

<verification>
Run the ML validation suite and compare its fold semantics directly against the baseline holdout harness.
</verification>

<success_criteria>
`ML-02` is reachable because a conservative XGBoost candidate can now be trained and scored under the same temporal discipline as the existing baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ml-ensemble-upgrade-gated/05-02-SUMMARY.md`
</output>
