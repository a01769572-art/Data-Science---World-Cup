---
phase: 05-ml-ensemble-upgrade-gated
plan: "06"
type: execute
wave: 6
depends_on:
  - "05-05"
files_modified:
  - tests/test_ml_validation.py
  - src/cdd_mundial/models/ml_validation.py
autonomous: true
gap_closure: true
requirements:
  - ML-03
  - ML-04
decision_ids:
  - D-11
  - D-12
  - D-13
must_haves:
  truths:
    - "El guardrail de regresion CR-01 prueba IDENTIDAD de modelo/distribucion, no un conteo de fits: aseverar, por holdout, que el array que ml_calibrator.transform recibe al puntuar el holdout es una salida verbatim de predict_proba del MISMO modelo cuyo raw alimento ml_calibrator.fit."
    - "El guardrail FALLA ante el probe del verificador — distorsionar ml_holdout_raw (x^3 + renormalizar) que NO agrega ningun fit() — porque el array transformado deja de ser una salida verbatim de predict_proba del modelo de calibracion."
    - "El guardrail SIGUE atrapando el defecto literal CR-01 (re-fit de un final_model entre calibracion y scoring) y permanece verde contra el codigo de produccion correcto (scoring_model == inner_model)."
  artifacts:
    - path: "tests/test_ml_validation.py"
      provides: "Regresion CR-01 reescrita que captura la identidad del array de scoring del holdout (no solo el conteo de fits) y la compara, holdout a holdout, contra el modelo cuyas salidas se calibraron; instrumentacion output_to_model/fit_inputs ahora load-bearing en la asercion."
    - path: "src/cdd_mundial/models/ml_validation.py"
      provides: "SOLO si es estrictamente necesario para observar la identidad: una costura de prueba minima y sin cambio de comportamiento (p.ej. exponer el id/handle del modelo de scoring por holdout). No tocar la logica del gate ni el flujo de produccion."
  key_links:
    - from: "tests/test_ml_validation.py"
      to: "src/cdd_mundial/models/ml_calibration.py"
      via: "monkeypatch a MulticlassCalibrator.transform captura id(array) al puntuar holdout y lo cruza con output_to_model"
      pattern: "MulticlassCalibrator\\.transform|transform|output_to_model"
    - from: "tests/test_ml_validation.py"
      to: "src/cdd_mundial/models/ml_xgboost.py"
      via: "predict_proba tracking mapea id(array de salida) -> id(modelo); la salida de scoring debe trazar al modelo de calibracion-fit"
      pattern: "predict_proba|output_to_model|id\\("
    - from: "tests/test_ml_validation.py"
      to: "src/cdd_mundial/models/ml_validation.py"
      via: "guardrail de identidad train/serve por holdout para CR-01 (no conteo de fits)"
      pattern: "run_ml_comparison|ml_holdout_raw|scoring_model"
---

<objective>
Cerrar el ultimo gap de la Phase 5 (WR-01 / VERIFICATION status `gaps_found`, 3/4): el guardrail de regresion CR-01 NO prueba el invariante de identidad que su docstring afirma — solo cuenta fits por holdout (`n_models == n_holdouts`), un proxy fragil. El verificador demostro empiricamente que un mismatch train/serve SIN fit extra (distorsionar `ml_holdout_raw` con x^3 + renormalizar) pasa el test cuando debe fallarlo.

El codigo de produccion en `run_ml_comparison` YA es correcto (`scoring_model = inner_model`, `final_model` eliminado, 0 coincidencias en `src/`). Este plan es de alcance estrictamente de TEST: fortalecer la regresion para que asevere identidad de modelo/distribucion real, atrapando el probe exacto del verificador, sin tocar la logica del gate ni ningun comportamiento de produccion.

Purpose: restaurar la red de seguridad metodologica que blinda `ML-03` (gate pre-registrado) y `ML-04` (isotonic-vs-Platt empirico) contra la reaparicion silenciosa del defecto que invalido la verificacion previa.
Output: un guardrail de regresion CR-01 que demuestra RED contra un mismatch sin fit extra y GREEN contra el codigo de produccion correcto; comandos de re-verificacion enfocados con el interprete `.\.venv\Scripts\python.exe`.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-VERIFICATION.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-REVIEW.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-05-PLAN.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-05-SUMMARY.md
@src/cdd_mundial/models/ml_validation.py
@src/cdd_mundial/models/ml_calibration.py
@src/cdd_mundial/models/ml_xgboost.py
@tests/test_ml_validation.py

<interfaces>
Flujo de scoring del holdout en `src/cdd_mundial/models/ml_validation.py` (paso 4 de `run_ml_comparison`, lineas ~393-444):

```python
inner_model = MulticlassXGBoost(seed=seed).fit(x_fit, y_fit)   # un solo modelo por holdout
ml_fit_raw = inner_model.predict_proba(x_fit)                  # raw sobre el que se ajusta el calibrador
ml_calibrator = MulticlassCalibrator(method=ml_choice["method"]).fit(ml_fit_raw, y_fit)
...
scoring_model = inner_model                                    # CR-01: identidad train/serve
ml_holdout_raw = scoring_model.predict_proba(x_holdout)        # raw que se transforma al puntuar
ml_holdout = ml_calibrator.transform(ml_holdout_raw)           # <-- punto a instrumentar
```

Costura de calibracion (`src/cdd_mundial/models/ml_calibration.py`):

```python
class MulticlassCalibrator:
    def fit(self, probs: np.ndarray, y: np.ndarray) -> "MulticlassCalibrator": ...
    def transform(self, probs: np.ndarray) -> np.ndarray: ...   # recibe el raw de scoring
```

Campos de auditoria por-holdout YA presentes en el reporte (lineas ~468-470): `"scoring_model"`, `"n_inner_fit"`, `"n_inner_cal"`. Usar estos como seam si se necesita exponer identidad; NO inventar campos que alteren el comportamiento.

El probe del verificador a defenderse (status partial en VERIFICATION.md, "Behavioral Spot-Checks", fila ✗ FAIL):
```text
Distorsionar ml_holdout_raw (elevar al cubo + renormalizar) de modo que la distribucion
transformada por los calibradores difiera de aquella sobre la que se ajustaron, SIN agregar
ninguna llamada fit() extra. El guard actual PASA (1 passed) cuando debe FALLAR.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — demostrar que el guardrail actual NO atrapa el mismatch sin fit extra</name>
  <read_first>
    - tests/test_ml_validation.py  (funcion test_comparison_scores_holdout_with_the_same_model_calibrators_were_fit_on, lineas 427-510)
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-VERIFICATION.md  (bloque `gaps:` y "Behavioral Spot-Checks": el probe x^3+renorm)
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-REVIEW.md  (WR-01)
    - src/cdd_mundial/models/ml_validation.py  (lineas 393-444: ml_fit_raw, scoring_model, ml_holdout_raw, ml_calibrator.transform)
  </read_first>
  <files>tests/test_ml_validation.py</files>
  <behavior>
    - El probe del verificador (distorsionar `ml_holdout_raw` con `arr**3` + renormalizar, monkeypatch sobre `MulticlassXGBoost.predict_proba` SOLO para el array de scoring del holdout, SIN agregar ningun `fit()`) debe hacer FALLAR la version fortalecida del test.
    - La asercion load-bearing actual (`assert fit_models` no-vacio + `n_models == n_holdouts`) NO atrapa ese probe: documentar el resultado RED actual (test pasa con el probe → bug) antes de fortalecer.
  </behavior>
  <action>
    Reproducir y dejar registrado el RED del verificador como punto de partida, dentro de `tests/test_ml_validation.py`:

    1. Crear un test temporal-de-prueba `test_PROBE_distortion_without_extra_fit_must_fail` (se ELIMINA al final de Task 2; existe solo para demostrar el RED) que: (a) aplica el mismo monkeypatch de tracking de `test_comparison_scores_holdout_with_the_same_model_calibrators_were_fit_on`, y ADEMAS (b) envuelve `MulticlassXGBoost.predict_proba` para que, cuando el numero de filas del array de entrada coincida con el del holdout (`x_holdout`), devuelva `out_distorted = (out**3); out_distorted /= out_distorted.sum(axis=1, keepdims=True)` — es decir, distorsiona EXCLUSIVAMENTE el `ml_holdout_raw`, sin agregar ninguna llamada `fit()` (el conteo `n_models == n_holdouts` sigue intacto).
    2. Ejecutar el guardrail ACTUAL contra ese dataset distorsionado y confirmar por evidencia que PASA (el bug WR-01: el guard no bita). Anotar en un comentario `# RED probe (WR-01): el conteo de fits NO detecta este mismatch` el resultado observado.
    3. NO modificar aun la asercion del test real en este paso; el objetivo de Task 1 es congelar el probe exacto que Task 2 debe hacer fallar. Identificar el punto de instrumentacion correcto: el array que `ml_calibrator.transform` recibe al puntuar el holdout (linea ~439 de `ml_validation.py`) debe ser una salida VERBATIM de `predict_proba` (su `id` debe estar en `output_to_model`); el probe lo rompe porque `out_distorted` es un array DERIVADO, no una salida verbatim del modelo de calibracion.

    Trabajo de SOLO test: no tocar `src/` en esta tarea.
  </action>
  <acceptance_criteria>
    - `tests/test_ml_validation.py` contiene `test_PROBE_distortion_without_extra_fit_must_fail` con el monkeypatch de distorsion x^3+renorm aplicado solo al array de scoring del holdout y SIN ningun `fit()` adicional (verificable por grep: `out\*\*3` y ausencia de un segundo `.fit(` en el cuerpo del probe).
    - Evidencia capturada de que el guardrail ACTUAL (`n_models == n_holdouts`) PASA bajo ese probe — confirmando el bug WR-01 que Task 2 debe cerrar.
    - `grep -v '^#' tests/test_ml_validation.py | grep -c 'def test_PROBE_distortion_without_extra_fit_must_fail'` == 1.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_ml_validation.py -k "PROBE_distortion or same_model" -x</automated>
  </verify>
  <done>Queda congelado, como codigo ejecutable, el probe exacto del verificador (mismatch de distribucion sin fit extra) y se demuestra que el guardrail actual no lo atrapa — el RED objetivo de la correccion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — fortalecer el guardrail a identidad de array/modelo por holdout y revalidar</name>
  <read_first>
    - tests/test_ml_validation.py  (resultado de Task 1, incluido el probe)
    - src/cdd_mundial/models/ml_calibration.py  (MulticlassCalibrator.fit/transform, lineas 108-154)
    - src/cdd_mundial/models/ml_validation.py  (campos audit scoring_model/n_inner_fit/n_inner_cal, lineas 464-470)
  </read_first>
  <files>tests/test_ml_validation.py, src/cdd_mundial/models/ml_validation.py</files>
  <behavior>
    - Tras fortalecer la asercion, el probe de Task 1 (distorsion sin fit extra) hace FALLAR a `test_comparison_scores_holdout_with_the_same_model_calibrators_were_fit_on`.
    - El defecto literal CR-01 (re-introducir un `final_model` re-fit entre calibracion y scoring → un segundo `id(modelo)` por holdout) SIGUE haciendo fallar el test.
    - Contra el codigo de produccion correcto (`scoring_model == inner_model`, sin distorsion), el test PASA en verde.
  </behavior>
  <action>
    Reescribir la asercion load-bearing de `test_comparison_scores_holdout_with_the_same_model_calibrators_were_fit_on` para que pruebe IDENTIDAD, no conteo:

    1. Hacer load-bearing la instrumentacion ya construida (`output_to_model`, `tracking_predict`). El mapa `output_to_model[id(out)] = id(self)` asocia cada salida verbatim de `predict_proba` a su modelo productor.
    2. Capturar el array que `ml_calibrator.transform` recibe al puntuar el holdout: hacer monkeypatch a `cdd_mundial.models.ml_validation.MulticlassCalibrator.transform` con un `tracking_transform(self, probs)` que registre `id(probs)` por llamada en una lista `transform_inputs`. (Alternativa equivalente: si una costura minima en `src/` facilita observar el array de scoring sin cambiar comportamiento — p.ej. exponer en el reporte por-holdout el `id`/handle del array `ml_holdout_raw` usando los campos audit existentes `scoring_model`/`n_inner_fit` — implementarla SOLO si el monkeypatch puro no basta; debe ser estrictamente no-funcional y no tocar el gate.)
    3. ASERCION CORE (sustituye a `assert fit_models` y a `n_models == n_holdouts` como unica prueba): el array que alimenta la `transform` de scoring del holdout para ML debe ser una salida VERBATIM de `predict_proba` (su `id` ∈ `output_to_model`) y su modelo productor (`output_to_model[id(probs)]`) debe ser EL MISMO `id(modelo)` cuyo raw (`ml_fit_raw`) alimento `ml_calibrator.fit` para ESE holdout. Es decir: `scoring_model_id == calibration_fit_model_id`, comparando identidad de objeto, holdout a holdout.
       - Esto bita el probe de Task 1: `out_distorted` (x^3+renorm) es un array derivado cuyo `id` NO esta en `output_to_model` → la asercion "el array de scoring es una salida verbatim del modelo de calibracion" FALLA, exactamente como debe.
       - El conteo `n_models == n_holdouts` puede CONSERVARSE como asercion COMPLEMENTARIA (no la unica), para seguir atrapando el patron literal `final_model`.
    4. Cruzar identidades por-holdout, no agregado sobre los cuatro: instrumentar de modo que cada holdout verifique su propio par (modelo-de-calibracion, modelo-de-scoring). Si el orden de llamadas lo permite, emparejar por secuencia de holdout; documentar el emparejamiento en un comentario.
    5. ELIMINAR `test_PROBE_distortion_without_extra_fit_must_fail` (era andamiaje RED) y, en su lugar, dejar dentro del test real un sub-bloque o un test hermano `test_comparison_guard_fails_on_distribution_mismatch_without_extra_fit` que aplique el probe x^3+renorm y use `pytest.raises(AssertionError)` (o equivalente) para PROBAR que el guard ahora FALLA ante el probe — convirtiendo el RED de Task 1 en una asercion permanente y verde.
    6. Re-ejecutar las suites Phase 5 enfocadas para confirmar cero regresion.

    Si se toca `src/cdd_mundial/models/ml_validation.py`: limitarse a una costura de observabilidad sin cambio de comportamiento (no alterar `evaluate_ml_gate`, ni el winner, ni los probs servidos); confirmar con `grep -c final_model src/cdd_mundial/models/ml_validation.py` == 0 que no se reintroduce el defecto.
  </action>
  <acceptance_criteria>
    - La asercion load-bearing compara identidad de objeto: `grep -n "output_to_model\[id" tests/test_ml_validation.py` muestra el cruce scoring-array → modelo, y existe una comparacion del tipo `assert scoring_model_id == calibration_fit_model_id` (o equivalente por-holdout) — verificable por grep.
    - `test_PROBE_distortion_without_extra_fit_must_fail` ya NO existe; en su lugar `grep -c 'def test_comparison_guard_fails_on_distribution_mismatch_without_extra_fit' tests/test_ml_validation.py` == 1 y ese test usa `pytest.raises` para probar el FALLO del guard ante el probe x^3+renorm sin fit extra.
    - Contra produccion correcta: `.\.venv\Scripts\python.exe -m pytest -q tests/test_ml_validation.py -k "same_model or guard_fails" -x` → todo verde.
    - `grep -c final_model src/cdd_mundial/models/ml_validation.py` == 0 (no se reintrodujo el defecto literal CR-01).
    - Si se toco `src/`: el winner del gate y los probs servidos son identicos antes/despues (cambio estrictamente de observabilidad).
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_ml_validation.py tests/test_ml_calibration.py -x</automated>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_ml_validation.py tests/test_ml_features.py tests/test_ml_selection.py -k "ml or selection or comparison or same_model or guard_fails" -x</automated>
  </verify>
  <done>El guardrail CR-01 prueba identidad real de modelo/distribucion: falla ante el probe de mismatch-sin-fit-extra del verificador y ante el re-fit literal de `final_model`, y queda verde contra el codigo de produccion correcto. La red de seguridad metodologica de `ML-03`/`ML-04` esta restaurada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codigo de produccion correcto -> guardrail de regresion | El guard es la unica barrera automatizada que impide la reaparicion silenciosa de CR-01 (mismatch train/serve) en un cambio futuro |
| array de scoring del holdout -> ml_calibrator.transform | La distribucion que el calibrador transforma al puntuar debe ser identica (mismo objeto/productor) a aquella sobre la que el calibrador fue ajustado |
| guardrail verde -> confianza en el veredicto del gate | Un guard que pasa debe significar que el gate consumio insumos honestamente calibrados, no solo que el conteo de fits cuadro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | T | `run_ml_comparison` identidad train/serve | mitigate | Mantenida por 05-05 en produccion (`scoring_model = inner_model`); este plan agrega el guard que la verifica por identidad de objeto, no por conteo. Confirmar `grep -c final_model src/...` == 0. |
| T-05-14 | R | integridad del guardrail de regresion CR-01 | mitigate | El guard debe FALLAR ante el probe x^3+renorm sin fit extra (WR-01): aseverar que el array de `ml_calibrator.transform` de scoring es una salida verbatim de `predict_proba` del MISMO modelo de calibracion-fit, por holdout — no un proxy de conteo `n_models == n_holdouts`. |
| T-05-15 | I | integridad del resultado negativo del gate | mitigate | Re-ejecutar suites de comparacion/seleccion/live para que un veredicto no-promocion reparado siga siendo explicito y confiable bajo D-13; el guard fortalecido evita que un mismatch futuro corrompa silenciosamente esos insumos. |
| T-05-16 | T | costura de observabilidad en `src/` (si se usa) | mitigate | Cualquier seam agregado a `ml_validation.py` debe ser estrictamente no-funcional: winner del gate y probs servidos identicos antes/despues; no tocar `evaluate_ml_gate`. Verificar por igualdad de salida. |
</threat_model>

<verification>
1. Probar RED primero (Task 1): el probe del verificador (distorsion x^3+renorm de `ml_holdout_raw` sin fit extra) demuestra que el guard de conteo no bita.
2. Tras fortalecer (Task 2): el mismo probe hace FALLAR el guard (convertido en asercion permanente via `pytest.raises`), y el patron literal `final_model` tambien lo hace fallar.
3. Contra el codigo de produccion correcto, toda la suite Phase 5 queda verde:
   `.\.venv\Scripts\python.exe -m pytest -q tests/test_ml_validation.py tests/test_ml_calibration.py tests/test_ml_features.py tests/test_ml_selection.py -x`
4. Nota de entorno: usar `.\.venv\Scripts\python.exe` — `.\.venv\python.exe` es un stub deshidratado de OneDrive y no debe usarse.
5. La reparacion esta completa solo si el guard fortalecido es verde contra produccion Y rojo (via `pytest.raises`) contra el probe de mismatch-sin-fit-extra, sin tocar la logica del gate.
</verification>

<success_criteria>
`ML-03` y `ML-04` quedan honestamente blindados solo cuando el guardrail de regresion CR-01 prueba la identidad de modelo/distribucion train/serve por holdout (el array de scoring del holdout es una salida verbatim de `predict_proba` del mismo modelo cuyas salidas se calibraron) y FALLA ante el probe de mismatch sin fit extra que el verificador uso para confirmar WR-01 — sin alterar ninguna logica del gate ni comportamiento de produccion.
</success_criteria>

<out_of_scope>
- **WR-02 (decision del Director, NO una tarea de codigo aqui):** el fix de 05-05 puntua el holdout con `inner_model` (~75% de filas pre-cutoff) mientras el baseline Dixon-Coles usa el 100%, introduciendo una asimetria baseline-vs-ML en el gate. Es metodologicamente defendible (prioriza identidad train/serve) pero el "honestamente" del goal de fase tiene este matiz. Jesus (Director) deberia registrarlo explicitamente — documentar la limitacion en el reporte del gate o adoptar un patron out-of-fold que preserve ambas propiedades. Se deja como nota; este plan NO lo aborda como codigo.
- Pins de dependencias (pandas ~=2.3.3, xgboost ~=3.2): son guardrails tournament-safe; NO modificar.
- Logica del gate (`evaluate_ml_gate`), winner, o cualquier comportamiento de produccion: fuera de alcance.
</out_of_scope>

<execution_notes>
Advertencias del plan-checker (no bloquean, pero atender en ejecucion — la #1 puede REPRODUCIR el gap si se ignora):

1. **Orden de composicion de monkeypatches (CRITICO para el RED).** El probe envuelve `MulticlassXGBoost.predict_proba` para devolver `out_distorted`, y `tracking_predict` tambien envuelve `predict_proba` registrando `id(out)->id(self)` en `output_to_model`. Si el wrapper de distorsion se aplica POR ENCIMA de `tracking_predict`, entonces `output_to_model` registraria el `id` del array YA distorsionado y la asercion "`id(probs) ∈ output_to_model`" pasaria por la via equivocada — el test volveria a no morder el probe (gap reproducido). Mitigacion: anclar la asercion en el invariante verbatim-output-del-modelo-de-CALIBRACION (comparar contra `id(ml_fit_raw)` exacto que alimento `ml_calibrator.fit`), NO meramente "esta en output_to_model". Idealmente añadir asercion auxiliar: con el probe activo, `id(ml_holdout_raw) != id(cualquier salida verbatim del modelo de calibracion)`, para que el RED sea inequivoco.

2. **Emparejamiento por OBJETO, no por secuencia de llamadas.** Cruzar (modelo-de-calibracion, modelo-de-scoring) por holdout via indice de secuencia (listas `fit_inputs`/`transform_inputs`) es la misma fragilidad de proxy que el gap denuncia: un cambio futuro en el orden de operaciones dentro del bucle (p.ej. mover `ens_calibrator.fit`) desalinearia el emparejamiento en silencio. Preferir capturar `id(inner_model)` por holdout via hook en `MulticlassXGBoost.fit` y cruzarlo directamente contra `output_to_model[id(probs)]` del array de scoring de ESE holdout.

3. **Sintaxis grep en acceptance_criteria es POSIX; el entorno es PowerShell (Windows).** Los `<verify>` con pytest son correctos. Tratar los `grep -v`/`grep -c` de los acceptance_criteria como pseudo-criterios conceptuales o traducir a `Select-String` en ejecucion. Cosmetico, no afecta la logica del test.
</execution_notes>

<output>
After completion, create `.planning/phases/05-ml-ensemble-upgrade-gated/05-06-SUMMARY.md`
</output>
