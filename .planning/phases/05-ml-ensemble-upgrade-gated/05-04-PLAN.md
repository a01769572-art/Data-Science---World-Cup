---
phase: 05-ml-ensemble-upgrade-gated
plan: "04"
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - src/cdd_mundial/live/pipeline.py
  - src/cdd_mundial/live/report.py
  - src/cdd_mundial/live/ml_selection.py
  - tests/test_live_pipeline.py
  - tests/test_live_report.py
  - notebooks/05_ml_ensemble.ipynb
  - tests/test_notebooks.py
autonomous: true
requirements:
  - ML-03
decision_ids:
  - D-13
  - D-14
must_haves:
  truths:
    - "Aunque el upgrade gane, la publicacion operativa sigue siendo dual: baseline mas candidato promovido."
    - "Si el gate no pasa o el partido es inelegible para ML, la prediccion publicada cae de forma explicita al baseline."
    - "La salida en vivo debe dejar trazabilidad de que modelo produjo cada fila publicada y por que."
  artifacts:
    - path: "src/cdd_mundial/live/ml_selection.py"
      provides: "Decision explicita baseline-vs-ml-vs-ensemble para publicacion dual y fallback"
    - path: "src/cdd_mundial/live/pipeline.py"
      provides: "Integracion del candidato promovido sin romper el camino baseline existente"
    - path: "src/cdd_mundial/live/report.py"
      provides: "Reporte visible que distingue baseline y upgrade o documenta shadow/no-promotion"
    - path: "notebooks/05_ml_ensemble.ipynb"
      provides: "Artefacto didactico con evidencia del gate, comparacion de candidatos y resultado final"
  key_links:
    - from: "src/cdd_mundial/live/ml_selection.py"
      to: "src/cdd_mundial/live/pipeline.py"
      via: "seleccion por partido del modelo publicado y fallback al baseline"
      pattern: "model_version|reused_model|published"
    - from: "src/cdd_mundial/live/report.py"
      to: "src/cdd_mundial/live/calibration.py"
      via: "continuidad del ledger/calibracion al agregar una segunda familia de prediccion"
      pattern: "model_version|calibration"
---

<objective>
Integrar el resultado de Phase 5 al pipeline vivo sin degradar la estabilidad del baseline.

Purpose: volver operacional el gate de upgrade bajo publicacion dual y fallback explicito.
Output: seleccion live baseline/ML/ensemble por partido, metadata/reportes claros y notebook final de evidencia.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-03-PLAN.md
@src/cdd_mundial/live/pipeline.py
@src/cdd_mundial/live/report.py
@src/cdd_mundial/live/calibration.py
@tests/test_live_pipeline.py
@tests/test_live_report.py
@notebooks/02_modelos_baseline.ipynb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate dual-publication selection and explicit baseline fallback</name>
  <read_first>
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
    - src/cdd_mundial/live/pipeline.py
    - src/cdd_mundial/live/report.py
  </read_first>
  <files>src/cdd_mundial/live/ml_selection.py, src/cdd_mundial/live/pipeline.py, tests/test_live_pipeline.py</files>
  <action>Add a small production selection layer that reads the Phase 5 gate output and decides, per match, whether publication uses baseline only, the promoted candidate, or baseline fallback because the row is ML-ineligible. Keep the existing baseline publication path intact. Record enough metadata to explain the decision for every published row and to prove that no failed or ineligible candidate silently displaced the baseline.</action>
  <acceptance_criteria>The official run can publish dual candidate outputs or baseline-only fallback without ambiguity; tests prove ineligible rows and failed-gate phases revert to the baseline path explicitly.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_pipeline.py -x</automated>
  </verify>
  <done>The live path can consume a promoted Phase 5 candidate without risking hidden regressions in the baseline publication workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the Phase 5 decision clearly in reports and notebook evidence</name>
  <read_first>
    - src/cdd_mundial/live/report.py
    - notebooks/02_modelos_baseline.ipynb
    - tests/test_notebooks.py
  </read_first>
  <files>src/cdd_mundial/live/report.py, tests/test_live_report.py, notebooks/05_ml_ensemble.ipynb, tests/test_notebooks.py</files>
  <action>Extend the reporting layer so the published output distinguishes baseline from upgrade and documents the gate outcome. Add one executed didactic notebook for Phase 5 that imports production APIs, explains the feature table, ML candidate, calibration, ensemble comparison, and final promotion/no-promotion result. Preserve the repo's Markdown -> code -> Markdown structure and commit non-empty deterministic outputs from a lightweight run.</action>
  <acceptance_criteria>The live report and notebook make the upgrade decision legible to a reviewer without reading raw code. If the gate fails, that negative result is visible and the baseline continuity is explained. If the gate passes, the promoted family and dual-publication semantics are visible.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_report.py tests/test_notebooks.py -x</automated>
  </verify>
  <done>Phase 5 closes with operational clarity and portfolio-quality evidence instead of a hidden model swap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation verdict -> live publication | A backtest decision becomes a production-path selector |
| live prediction rows -> published report | Technical model selection becomes user-facing evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | T | live model selection | mitigate | Centralize baseline/upgrade/fallback logic in one explicit production module with tests. |
| T-05-11 | R | published model provenance | mitigate | Record model family and fallback reason per row/summary so publication remains auditable. |
| T-05-12 | I | hidden negative result | mitigate | Surface no-promotion outcomes in reports and notebook evidence instead of burying them in raw artifacts. |
</threat_model>

<verification>
Run the live pipeline/report and notebook suites and confirm the published output stays interpretable whether the gate passes or fails.
</verification>

<success_criteria>
Phase 5 is operationally complete because any promoted candidate enters the live system through explicit dual publication and fallback-safe reporting rather than a fragile replacement path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ml-ensemble-upgrade-gated/05-04-SUMMARY.md`
</output>
