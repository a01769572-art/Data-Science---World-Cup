---
phase: 05-ml-ensemble-upgrade-gated
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/cdd_mundial/models/ml_calibration.py
  - src/cdd_mundial/models/ml_validation.py
  - tests/test_ml_calibration.py
  - tests/test_ml_validation.py
autonomous: true
requirements:
  - ML-03
  - ML-04
decision_ids:
  - D-10
  - D-11
  - D-12
  - D-13
must_haves:
  truths:
    - "La comparacion metodologica de la fase incluye baseline, ML solo y ensemble."
    - "ML solo y ensemble se calibran por separado dentro de validacion temporal; la calibracion no se aplica retroactivamente sobre holdouts ya vistos."
    - "La promocion del upgrade solo ocurre si un candidato vence al baseline en log-loss en los cuatro holdouts."
  artifacts:
    - path: "src/cdd_mundial/models/ml_calibration.py"
      provides: "Wrappers de calibracion multiclass y utilidades para comparar sigmoid vs isotonic"
    - path: "src/cdd_mundial/models/ml_validation.py"
      provides: "Comparacion baseline vs ML solo vs ensemble y gate explicito de promocion"
    - path: "tests/test_ml_calibration.py"
      provides: "Pruebas de calibracion temporal y conservacion de distribuciones validas"
    - path: "tests/test_ml_validation.py"
      provides: "Pruebas del gate de 4 holdouts, artifacts de reporte y resultado negativo explicito"
  key_links:
    - from: "src/cdd_mundial/models/ml_validation.py"
      to: "src/cdd_mundial/models/validation.py"
      via: "paralelo de evaluate_gate y reportes por holdout"
      pattern: "evaluate_gate|per_holdout"
    - from: "src/cdd_mundial/models/ml_validation.py"
      to: "src/cdd_mundial/models/dixon_coles.py"
      via: "baseline probabilistico productivo como comparador y componente del ensemble"
      pattern: "wdl_from_lambdas|predict_lambdas"
---

<objective>
Comparar candidatos calibrados y registrar el gate de promocion del upgrade.

Purpose: cerrar `ML-03` y `ML-04` como una decision auditable, aunque el resultado sea no promover.
Output: calibracion multiclass temporal, ensemble simple y un reporte de gate que nombre al ganador o documente el no-upgrade.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-02-PLAN.md
@src/cdd_mundial/models/validation.py
@src/cdd_mundial/models/ml_validation.py
@tests/test_validation_temporal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multiclass temporal calibration for ML solo and ensemble</name>
  <read_first>
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
    - src/cdd_mundial/models/ml_validation.py
  </read_first>
  <files>src/cdd_mundial/models/ml_calibration.py, tests/test_ml_calibration.py, tests/test_ml_validation.py</files>
  <action>Implement the calibration utilities required by D-11 and D-12. Compare sigmoid/Platt versus isotonic empirically inside temporal validation for ML solo and ensemble separately. Write RED tests first for probability-mass preservation, no use of future holdout rows when fitting calibrators, and artifact/report visibility of which calibrator won per candidate.</action>
  <acceptance_criteria>The repo can fit and evaluate calibrated ML solo and calibrated ensemble candidates without violating temporal boundaries, and tests prove all calibrated outputs remain valid 3-class distributions.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_calibration.py tests/test_ml_validation.py -k calibration -x</automated>
  </verify>
  <done>Phase 5 now compares calibrated candidates instead of assuming uncalibrated or isotonic-by-default results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode the ensemble comparison and 4-holdout promotion gate</name>
  <read_first>
    - src/cdd_mundial/models/validation.py
    - tests/test_validation_temporal.py
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
  </read_first>
  <files>src/cdd_mundial/models/ml_validation.py, tests/test_ml_validation.py</files>
  <action>Extend the validation harness to score three candidates per holdout: baseline, ML solo, ensemble. Implement a simple convex-weight ensemble over probability vectors and a pure gate function that passes only if the promoted candidate beats the baseline in log-loss on all four holdouts. Persist a dated report that includes per-holdout metrics, chosen calibrator/weight, and a first-class negative-result path when no candidate clears the gate.</action>
  <acceptance_criteria>The promotion decision is reproducible from code and artifacts alone. A failing candidate set still yields a valid report and leaves the baseline as the documented winner. A passing candidate set names the promoted model family unambiguously.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_validation.py -x</automated>
  </verify>
  <done>The upgrade gate is no longer narrative-only; it exists as executable phase evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ML/base probabilities -> calibrated candidates | Raw candidate probabilities become promoted or rejected contenders |
| holdout metrics -> promotion decision | Evaluation evidence becomes a production-go/no-go decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T | calibrator fitting | mitigate | Fit calibrators only on pre-holdout temporal data and assert this in tests. |
| T-05-08 | R | promotion gate | mitigate | Encode the four-holdout criterion in a pure function persisted in the dated report. |
| T-05-09 | I | negative result handling | mitigate | Treat “baseline wins” as a first-class successful outcome with explicit report fields, not an implicit absence of deployment. |
</threat_model>

<verification>
Run the calibration and ML validation suites and inspect the report schema for per-holdout candidate evidence plus the final gate verdict.
</verification>

<success_criteria>
`ML-03` and `ML-04` are structurally reachable because Phase 5 can now compare calibrated candidates honestly and decide promotion with an explicit, reproducible gate.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ml-ensemble-upgrade-gated/05-03-SUMMARY.md`
</output>
