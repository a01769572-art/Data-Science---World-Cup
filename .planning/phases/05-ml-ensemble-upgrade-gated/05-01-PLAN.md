---
phase: 05-ml-ensemble-upgrade-gated
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/cdd_mundial/models/ml_features.py
  - src/cdd_mundial/models/__init__.py
  - tests/test_ml_features.py
autonomous: true
requirements:
  - ML-01
decision_ids:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
must_haves:
  truths:
    - "La matriz ML v1 se construye punto-en-el-tiempo desde los mismos insumos canonicos que ya alimentan baseline y live."
    - "La elegibilidad ML exige al menos 5 partidos previos por equipo; cuando no se cumple, la fila queda auditada pero el camino ML no la consume."
    - "El dataset canonico de XGBoost conserva unidades naturales y no introduce normalizacion ni proxies opacos."
  artifacts:
    - path: "src/cdd_mundial/models/ml_features.py"
      provides: "Builder determinista del dataset point-in-time y metadata de cobertura/elegibilidad ML"
    - path: "tests/test_ml_features.py"
      provides: "Pruebas de cutoff estricto, set fijo de 12 features, ventana last_5 y fallback por cobertura insuficiente"
    - path: "pyproject.toml"
      provides: "Dependencia explicita de XGBoost y cualquier marker/test dependency necesaria para la fase"
  key_links:
    - from: "src/cdd_mundial/models/ml_features.py"
      to: "src/cdd_mundial/models/loading.py"
      via: "target 3-way canonico con semantica de 90 minutos"
      pattern: "outcome_idx|load_matches"
    - from: "src/cdd_mundial/models/ml_features.py"
      to: "src/cdd_mundial/models/dixon_coles.py"
      via: "features estructurales lambda/probabilidades del baseline"
      pattern: "predict_lambdas|wdl_from_lambdas"
    - from: "src/cdd_mundial/models/ml_features.py"
      to: "src/cdd_mundial/live/materialization.py"
      via: "consumo de frame historico o artifact live-training sin mutar historia"
      pattern: "materialize_live_training|_frame"
---

<objective>
Congelar la construccion del dataset supervisado ML v1 antes de escribir cualquier logica de modelado.

Purpose: cerrar `ML-01` como contrato de datos y anti-leakage, no como notebook exploratorio.
Output: builder point-in-time reproducible, set fijo de 12 features, metadata de elegibilidad/fallback, y pruebas que bloquean leakage temporal.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
@src/cdd_mundial/models/loading.py
@src/cdd_mundial/models/dixon_coles.py
@src/cdd_mundial/models/validation.py
@src/cdd_mundial/live/materialization.py
@data/processed/models/elo_history.parquet
@tests/test_validation_temporal.py

<interfaces>
From `src/cdd_mundial/models/loading.py`:

```python
def load_matches(path: Path = Path("data/processed/historical_matches.parquet"), frame: pd.DataFrame | None = None) -> pd.DataFrame: ...
```

From `src/cdd_mundial/models/dixon_coles.py`:

```python
def predict_lambdas(team_a: str, team_b: str, ctx: dict) -> tuple[float, float]: ...
def wdl_from_lambdas(lam: float, mu: float, rho: float, max_goals: int = 10) -> tuple[float, float, float]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the ML feature contracts and coverage semantics</name>
  <read_first>
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
    - src/cdd_mundial/models/loading.py
    - src/cdd_mundial/live/materialization.py
  </read_first>
  <files>pyproject.toml, src/cdd_mundial/models/ml_features.py, tests/test_ml_features.py</files>
  <action>Create a dedicated point-in-time dataset builder module for Phase 5. The builder must emit exactly the 12 fixed features from D-02, preserve `outcome_idx` from `load_matches(...)`, and include explicit coverage metadata that proves whether each row is ML-eligible under D-04. Implement the raw rolling features with a fixed `last_5` window and natural units only. Add RED tests first for: strict `date < kickoff` feature generation, fixed feature-column membership, `last_5` semantics, no normalization, and row retention plus ineligibility when either team lacks five prior matches.</action>
  <acceptance_criteria>The repo contains one canonical builder for the ML v1 table; the fixed 12-feature set is encoded in code/tests; the coverage/fallback rules are explicit and machine-verifiable instead of implied by notebooks.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_features.py -x</automated>
  </verify>
  <done>The Phase 5 supervised dataset is reproducible, point-in-time, and safe to feed into later holdout and live paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Support historical and live-training inputs from the same builder</name>
  <read_first>
    - src/cdd_mundial/models/validation.py
    - src/cdd_mundial/live/materialization.py
    - tests/test_validation_temporal.py
  </read_first>
  <files>src/cdd_mundial/models/ml_features.py, src/cdd_mundial/models/__init__.py, tests/test_ml_features.py</files>
  <action>Extend the builder so it can consume either the canonical historical parquet or a passed-in dated frame from the Phase 4 live-training path. Keep the logic pure: one builder, two sources, identical feature semantics. Export only the public helpers later plans need; do not embed training, calibration, or CLI orchestration here.</action>
  <acceptance_criteria>The same feature builder can be called in backtests and in production preparation without duplicating logic or changing column semantics; tests prove the source switch does not alter the feature contract.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_features.py -x</automated>
  </verify>
  <done>Later Phase 5 plans can reuse one deterministic feature path for both holdouts and live publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical match history -> ML feature table | Historical/live rows become supervised learning inputs |
| production baseline outputs -> ML feature table | Structural baseline rates cross into the meta-model feature space |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | rolling feature generation | mitigate | Assert strict `date < kickoff` windows and reject any look-ahead construction in tests. |
| T-05-02 | T | low-coverage matches | mitigate | Encode D-04 as explicit eligibility metadata and baseline fallback semantics, never silent row drops. |
| T-05-03 | R | optional FIFA ranking feature | mitigate | Keep it gated and absent-by-default unless a reproducible point-in-time source is added explicitly. |
</threat_model>

<verification>
Run the focused ML-feature suite and confirm the builder is the only path that defines the 12-feature matrix used later by training and live inference.
</verification>

<success_criteria>
`ML-01` is structurally reachable because the repo now has one deterministic, point-in-time, coverage-aware ML feature table rather than ad hoc feature logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ml-ensemble-upgrade-gated/05-01-SUMMARY.md`
</output>
