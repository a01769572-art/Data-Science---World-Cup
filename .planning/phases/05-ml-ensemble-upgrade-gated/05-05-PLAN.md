---
phase: 05-ml-ensemble-upgrade-gated
plan: "05"
type: execute
wave: 5
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - src/cdd_mundial/models/ml_validation.py
  - tests/test_ml_validation.py
  - tests/test_ml_calibration.py
autonomous: true
gap_closure: true
requirements:
  - ML-03
  - ML-04
decision_ids:
  - D-11
  - D-12
  - D-13
must_haves:
  truths:
    - "El gate de promocion usa log-losses de holdout calculados sobre probabilidades calibradas para la misma distribucion del modelo realmente puntuado."
    - "La comparacion empirica none-vs-Platt-vs-isotonic describe la distribucion que luego se transforma al puntuar holdouts; no existe mismatch train/serve entre calibrador y productor de probabilidades."
    - "Una regresion que vuelva a calibrar con un productor de probabilidades y puntuar con otro falla por test automatizado antes de afectar el veredicto del gate."
  artifacts:
    - path: "src/cdd_mundial/models/ml_validation.py"
      provides: "run_ml_comparison corregido para mantener identidad explicita y verificable entre el modelo usado para fit/calibration y el modelo usado para holdout scoring"
    - path: "tests/test_ml_validation.py"
      provides: "Prueba de regresion CR-01 que demuestra que el productor raw de ml_fit_raw/ml_cal_raw es el mismo productor raw transformado al puntuar holdout"
    - path: "tests/test_ml_calibration.py"
      provides: "Cobertura enfocada de calibracion/transformacion para el flujo corregido y su contrato de distribucion valida"
  key_links:
    - from: "src/cdd_mundial/models/ml_validation.py"
      to: "src/cdd_mundial/models/ml_calibration.py"
      via: "el calibrador elegido se ajusta y transforma probabilidades provenientes del mismo productor raw"
      pattern: "select_best_calibration|MulticlassCalibrator|transform"
    - from: "src/cdd_mundial/models/ml_validation.py"
      to: "src/cdd_mundial/models/ml_xgboost.py"
      via: "el modelo que genera ml_fit_raw/ml_cal_raw es tambien el que genera el holdout raw calibrado"
      pattern: "inner_model|final_model|predict_proba"
    - from: "tests/test_ml_validation.py"
      to: "src/cdd_mundial/models/ml_validation.py"
      via: "guardrail de identidad train/serve para CR-01"
      pattern: "run_ml_comparison|predict_proba|holdout"
---

<objective>
Cerrar el blocker CR-01 sin reabrir el resto de la fase: corregir la identidad entre modelo de calibracion y modelo de scoring dentro de `run_ml_comparison`, y dejar una regresion automatizada que impida repetir el mismatch.

Purpose: restaurar la validez metodologica del gate de promocion y de la comparacion isotonic-vs-Platt exigidas por `ML-03` y `ML-04`.
Output: flujo de comparacion ML/ensemble con identidad train/serve explicita, prueba de regresion CR-01, y comandos de re-verificacion enfocados para volver a confiar en el veredicto del gate.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-CONTEXT.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-RESEARCH.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-VERIFICATION.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-REVIEW.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-03-PLAN.md
@.planning/phases/05-ml-ensemble-upgrade-gated/05-04-PLAN.md
@src/cdd_mundial/models/ml_validation.py
@src/cdd_mundial/models/ml_calibration.py
@src/cdd_mundial/models/ml_xgboost.py
@tests/test_ml_validation.py
@tests/test_ml_calibration.py

<interfaces>
From `src/cdd_mundial/models/ml_validation.py`:

```python
def run_ml_comparison(
    dataset: pd.DataFrame | None = None,
    *,
    data_root: Path = Path("data"),
    seed: int = DEFAULT_SEED,
) -> tuple[dict[str, Any], pd.DataFrame]: ...

def evaluate_ml_gate(
    baseline_log_loss: dict[str, float],
    ml_log_loss: dict[str, float],
    ensemble_log_loss: dict[str, float],
) -> dict[str, Any]: ...
```

Current defect seam from `05-VERIFICATION.md` / `05-REVIEW.md`:

```text
Calibrators and ensemble weight are chosen on inner_model probabilities,
but holdout scoring transforms final_model probabilities. The gate then
compares baseline against challengers scored on a mismatched distribution.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair train/serve identity inside run_ml_comparison</name>
  <read_first>
    - src/cdd_mundial/models/ml_validation.py
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-VERIFICATION.md
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-REVIEW.md
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-03-PLAN.md
  </read_first>
  <files>src/cdd_mundial/models/ml_validation.py, tests/test_ml_validation.py</files>
  <action>Change `run_ml_comparison` so the raw probability producer used to generate `ml_fit_raw` and `ml_cal_raw` is also the raw probability producer whose holdout outputs are transformed by `ml_calibrator` and `ens_calibrator`. Prefer the localized repair direction from the gap report: score the holdout with that same already-tuned model, or otherwise make the train/serve model identity explicit in code and report fields if a different implementation is required. Do not leave a path where calibrators or `ensemble_weight` are tuned on `inner_model` but applied to a re-fit `final_model` without an explicit re-derivation step tied to that model's own raw probabilities.</action>
  <acceptance_criteria>`run_ml_comparison` no longer mixes calibrator-fit probabilities from one model instance with holdout probabilities from another. The per-holdout report remains reproducible and still records chosen calibrators/weight and the gate verdict for `baseline`, `ml`, and `ensemble`.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_validation.py -k "comparison or gate" -x</automated>
  </verify>
  <done>The gate inputs for ML and ensemble are methodologically valid again because their calibrated holdout probabilities come from the same model/distribution used to fit the calibration and ensemble-selection machinery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CR-01 regression proof and rerun focused verification suites</name>
  <read_first>
    - tests/test_ml_validation.py
    - tests/test_ml_calibration.py
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-VERIFICATION.md
    - .planning/phases/05-ml-ensemble-upgrade-gated/05-REVIEW.md
  </read_first>
  <files>tests/test_ml_validation.py, tests/test_ml_calibration.py</files>
  <action>Add an explicit regression test that fails if the raw probability producer used for calibration fit differs from the raw probability producer used for calibrated holdout scoring. The test should prove identity, not just shape or row-sum validity: instrument or stub the model path so the implementation must either reuse the same model instance for fit/calibration/holdout scoring or explicitly re-fit and re-derive calibrators/weight for the model that scores the holdout. Keep any added `tests/test_ml_calibration.py` coverage narrowly focused on the corrected flow's valid-probability contract and calibration method selection under the repaired path. After the regression guard exists, rerun the focused ML validation/calibration suites and, if still fast/stable, the adjacent Phase 5 verification-relevant tests that exercise the comparison output consumed by live selection.</action>
  <acceptance_criteria>A new automated test would have caught the original CR-01 behavior before verification. Focused suites pass against the repaired flow, and at least one verification command exercises the ML comparison/calibration path beyond the single regression test.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_validation.py tests/test_ml_calibration.py -x</automated>
    <automated>.\.venv\python.exe -m pytest -q tests/test_ml_selection.py tests/test_live_pipeline.py -k "ml or selection" -x</automated>
  </verify>
  <done>CR-01 is locked down by automated regression coverage and the Phase 5 comparison path has been revalidated far enough to trust the repaired gate output again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-holdout ML fit/calibration -> holdout scoring | Raw probabilities from the comparison model cross into calibrated holdout probabilities that determine the gate verdict |
| holdout metrics -> promotion decision | Correctly scored candidate metrics become a live go/no-go decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | T | `run_ml_comparison` calibration path | mitigate | Enforce same-producer train/serve identity for `ml_fit_raw`/`ml_cal_raw` and calibrated holdout scoring, per D-11/D-12 and CR-01. |
| T-05-14 | R | promotion gate evidence | mitigate | Add a regression test that proves the gate consumes holdout probabilities produced under the same calibration distribution rather than trusting structural validity alone. |
| T-05-15 | I | negative-result integrity | mitigate | Re-run focused comparison and live-selection suites so a repaired no-promotion verdict remains explicit and trustworthy under D-13. |
</threat_model>

<verification>
Re-run the focused ML comparison/calibration suites after the fix, then re-run the adjacent selection/pipeline tests that consume the comparison artifact if they stay feasible in under roughly one minute each. The repair is complete only if the new regression guard is green and the comparison path still emits a valid gate report.
</verification>

<success_criteria>
`ML-03` and `ML-04` are honestly satisfied only when `run_ml_comparison` scores holdouts on the same probability-producing model/distribution used to fit calibration and ensemble-selection inputs, and that invariant is enforced by automated regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ml-ensemble-upgrade-gated/05-05-SUMMARY.md`
</output>
