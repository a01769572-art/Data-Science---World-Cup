---
phase: 04-primer-pronostico-pipeline-diario
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/external/results_2026.csv
  - src/cdd_mundial/live/__init__.py
  - src/cdd_mundial/live/contracts.py
  - src/cdd_mundial/live/results.py
  - tests/test_live_results.py
autonomous: true
requirements:
  - DATA-06
  - LIVE-01
decision_ids:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-07
must_haves:
  truths:
    - "La corrida oficial parte de un `results_2026.csv` versionable y autoritativo."
    - "El pipeline construye `TournamentState` solo desde resultados validados contra el fixture oficial."
    - "Si falta un partido ya jugado o hay discrepancias con un scraper auxiliar, la corrida falla por defecto."
  artifacts:
    - path: "data/external/results_2026.csv"
      provides: "Fallback manual canonico y editable para resultados 2026"
    - path: "src/cdd_mundial/live/results.py"
      provides: "Carga, validacion y construccion fail-closed de TournamentState"
    - path: "tests/test_live_results.py"
      provides: "Pruebas del contrato CSV, completitud, override y discrepancias"
  key_links:
    - from: "data/external/results_2026.csv"
      to: "src/cdd_mundial/live/results.py"
      via: "pandera schema + parse a PlayedMatchResult"
      pattern: "results_2026.csv"
    - from: "src/cdd_mundial/live/results.py"
      to: "src/cdd_mundial/simulation/state.py"
      via: "TournamentState.from_results"
      pattern: "TournamentState\\.from_results"
---

<objective>
Definir el contrato canonico de resultados 2026 y la entrada validada del pipeline oficial.

Purpose: cerrar `DATA-06` primero para que ninguna corrida diaria dependa de scraping oportunista ni de notebooks.
Output: CSV canonico minimalista, esquemas `pandera` reutilizables para la capa `live`, cargador fail-closed a `TournamentState`, y pruebas de contrato.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
@.planning/phases/02-modelos-baseline/02-03-SUMMARY.md
@.planning/phases/03-simulador-del-torneo/03-05-SUMMARY.md
@src/cdd_mundial/simulation/state.py
@tests/test_tournament_state.py
@data/external/fixture_2026.csv
@data/external/odds_2026_template.csv

<interfaces>
From `src/cdd_mundial/simulation/state.py`:

```python
_REQUIRED_RESULT_KEYS = ("match_id", "team_a", "team_b", "goals_a", "goals_b")
_OPTIONAL_RESULT_KEYS = ("fair_play_a", "fair_play_b", "advanced_team")

class TournamentState:
    @classmethod
    def from_results(cls, results: Iterable[PlayedMatchResult], *, fixture: pd.DataFrame) -> TournamentState: ...
```

From `src/cdd_mundial/data/contracts.py` pattern:

```python
class CanonicalSchema(pa.DataFrameModel):
    class Config:
        strict = True
        coerce = True
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the live-input contracts and CSV fallback</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
    - src/cdd_mundial/simulation/state.py
    - data/external/odds_2026_template.csv
  </read_first>
  <files>data/external/results_2026.csv, src/cdd_mundial/live/__init__.py, src/cdd_mundial/live/contracts.py, tests/test_live_results.py</files>
  <action>Per D-01, D-03 and D-07, create the `cdd_mundial.live` package and freeze the canonical input contract before writing pipeline logic. Add a minimal versioned `data/external/results_2026.csv` whose columns are only those needed to build `TournamentState` (`match_id`, teams, goals, optional fair-play/advanced-team fields; no operational metadata). In `live/contracts.py`, define strict/coercing `pandera` schemas for live results plus downstream frozen benchmark/prediction/ledger tables so later plans can implement against a fixed interface without re-touching contracts. Write RED tests first for duplicate rows, unknown fixture matches, missing required columns, incomplete played-match coverage, and traced override semantics.</action>
  <acceptance_criteria>The repo contains a canonical empty-or-seeded `results_2026.csv` with no extra metadata columns; `live/contracts.py` exports strict schemas for results and downstream publication tables; the new tests fail before implementation and describe the exact Phase 4 invariants.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_results.py -x</automated>
  </verify>
  <done>`tests/test_live_results.py` passes and proves the contract blocks non-canonical or malformed live-result inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed results loading into TournamentState</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
    - src/cdd_mundial/simulation/state.py
    - tests/test_tournament_state.py
    - data/external/fixture_2026.csv
  </read_first>
  <files>src/cdd_mundial/live/results.py, src/cdd_mundial/live/__init__.py, tests/test_live_results.py</files>
  <action>Implement `live/results.py` so the official path is `results_2026.csv -> schema validation -> PlayedMatchResult rows -> TournamentState.from_results(...)`. Per D-04 and D-05, include explicit helpers for fixture-backed completeness checks, optional scraper-assist comparison that never overrides the CSV, and an override path that is opt-in and leaves a traceable record for later metadata capture. Keep the module pure and reusable by later plans; do not add CLI orchestration here.</action>
  <acceptance_criteria>Calling the live-results loader on the canonical CSV returns a valid `TournamentState`; duplicate or out-of-fixture rows raise loud errors; incomplete already-played matches fail unless an explicit override object/flag is passed; any auxiliary discrepancy is surfaced as a blocking error payload instead of silent repair.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_results.py tests/test_tournament_state.py -x</automated>
  </verify>
  <done>The live input layer is production-ready and every later Phase 4 plan can consume a stable `TournamentState` builder instead of parsing CSVs ad hoc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV input -> Python validation | Untrusted manual rows cross into the official publication path |
| Scraper assist -> canonical CSV comparison | External helper data must never silently rewrite canonical results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `results_2026.csv` | mitigate | Validate with strict `pandera` schemas and fixture-backed duplicate/existence checks per D-01/D-03. |
| T-04-02 | S | scraper-assist comparison | mitigate | Treat external assist data as verification-only and fail closed on mismatches per D-04. |
| T-04-03 | R | override path for incomplete played matches | mitigate | Require explicit override object/flag and emit traceable discrepancy details for later snapshot metadata per D-05. |
</threat_model>

<verification>
Run the focused live-results suite and confirm no direct parsing path bypasses `TournamentState.from_results(...)`.
</verification>

<success_criteria>
`DATA-06` is reachable because the authoritative live-results path is now versioned, validated, reproducible, and independent of any scraper's availability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-primer-pronostico-pipeline-diario/04-01-SUMMARY.md`
</output>
