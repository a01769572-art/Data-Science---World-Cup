---
phase: 04-primer-pronostico-pipeline-diario
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/cdd_mundial/live/materialization.py
  - src/cdd_mundial/live/predict.py
  - src/cdd_mundial/live/snapshots.py
  - src/cdd_mundial/live/pipeline.py
  - src/cdd_mundial/live/__main__.py
  - tests/test_live_snapshots.py
  - tests/test_live_pipeline.py
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - DOC-02
decision_ids:
  - D-02
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
must_haves:
  truths:
    - "Un comando oficial materializa primero un insumo derivado e inmutable de entrenamiento en vivo, luego refresca el estado/modelo, simula lo pendiente y produce un snapshot canonico."
    - "El snapshot oficial se ensambla en un directorio temporal hermano y solo se publica por rename atomico cuando el bundle completo esta listo."
    - "La corrida oficial reutiliza el artefacto de modelo vigente cuando el artefacto derivado y los parametros de features no cambiaron, y solo refitea cuando si cambiaron."
  artifacts:
    - path: "src/cdd_mundial/live/materialization.py"
      provides: "Materializacion inmutable del insumo canonico live-training y refresh cronologico de features Elo/forma"
    - path: "src/cdd_mundial/live/pipeline.py"
      provides: "Orquestacion oficial de corrida y resumen JSON"
    - path: "src/cdd_mundial/live/snapshots.py"
      provides: "Writer de staging/finalizacion atomica de snapshots con metadata y checksums"
    - path: "tests/test_live_pipeline.py"
      provides: "Pruebas del comando oficial, de la materializacion pre-refit/reuse y del publish atomico"
  key_links:
    - from: "src/cdd_mundial/live/materialization.py"
      to: "src/cdd_mundial/models/loading.py"
      via: "mapeo al schema canonico de partidos con semantica de 90 minutos"
      pattern: "load_matches|outcome_idx"
    - from: "src/cdd_mundial/live/materialization.py"
      to: "src/cdd_mundial/models/elo.py"
      via: "refresh cronologico de features Elo sobre filas historicas + live"
      pattern: "recompute_elo_history|snapshot_ratings"
    - from: "src/cdd_mundial/live/pipeline.py"
      to: "src/cdd_mundial/models/validation.py"
      via: "materializacion/refit/reuse sobre artefacto fechado y fingerprint de insumos"
      pattern: "materialize_validation|run_validation|verify_model04_materialization"
    - from: "src/cdd_mundial/live/pipeline.py"
      to: "src/cdd_mundial/simulation/engine.py"
      via: "simulate_tournaments con fixture completo y TournamentState"
      pattern: "simulate_tournaments"
    - from: "src/cdd_mundial/live/snapshots.py"
      to: "src/cdd_mundial/data/provenance.py"
      via: "sha256 y JSON determinista"
      pattern: "file_sha256|json.dumps"
---

<objective>
Construir el nucleo de la corrida oficial y el writer append-only del snapshot publicable.

Purpose: resolver la ruta critica hacia la primera publicacion oficial antes del 2026-06-27, sin redisenar modelos ni simulacion.
Output: predictor/orquestador oficial, regla determinista refit-vs-reuse, ensamblador atomico del snapshot bundle canonico, y CLI `python -m cdd_mundial.live`.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
@.planning/phases/02-modelos-baseline/02-03-SUMMARY.md
@.planning/phases/02-modelos-baseline/02-04-SUMMARY.md
@.planning/phases/03-simulador-del-torneo/03-05-SUMMARY.md
@src/cdd_mundial/models/dixon_coles.py
@src/cdd_mundial/models/loading.py
@src/cdd_mundial/models/elo.py
@src/cdd_mundial/models/validation.py
@src/cdd_mundial/simulation/engine.py
@src/cdd_mundial/simulation/outputs.py
@src/cdd_mundial/data/provenance.py
@tests/test_simulation_engine.py
@tests/test_provenance.py

<interfaces>
From `src/cdd_mundial/models/dixon_coles.py`:

```python
def load_production_model(models_dir: Path = Path("data/processed/models")) -> DixonColesModel: ...
def predict_lambdas(team_a: str, team_b: str, ctx: dict) -> tuple[float, float]: ...
```

From `src/cdd_mundial/models/validation.py`:

```python
def materialize_validation(data_root: Path = Path("data")) -> dict[str, Any]: ...
def verify_model04_materialization(data_root: Path = Path("data")) -> dict[str, Any]: ...
```

From `src/cdd_mundial/models/loading.py` and `src/cdd_mundial/models/elo.py`:

```python
def load_matches(path: Path = Path("data/processed/historical_matches.parquet"), frame: pd.DataFrame | None = None) -> pd.DataFrame: ...
def recompute_elo_history(matches: pd.DataFrame) -> pd.DataFrame: ...
def snapshot_ratings(history: pd.DataFrame, source_version: str) -> pd.DataFrame: ...
```

From `src/cdd_mundial/simulation/engine.py` and `outputs.py`:

```python
def simulate_tournaments(..., state: TournamentState, predict_lambdas, n_sims: int, seed: int, ctx: dict | None = None) -> SimulationResult: ...
def advancement_table(result: SimulationResult) -> pd.DataFrame: ...
def group_position_table(result: SimulationResult) -> pd.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Materialize immutable live-training inputs before deterministic model reuse/refit</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
    - src/cdd_mundial/models/dixon_coles.py
    - src/cdd_mundial/models/validation.py
    - src/cdd_mundial/models/loading.py
    - src/cdd_mundial/models/elo.py
    - src/cdd_mundial/simulation/engine.py
    - src/cdd_mundial/simulation/outputs.py
    - src/cdd_mundial/data/provenance.py
  </read_first>
  <files>src/cdd_mundial/live/materialization.py, src/cdd_mundial/live/predict.py, src/cdd_mundial/live/pipeline.py, tests/test_live_pipeline.py</files>
  <action>Per D-06 and the revision lock for LIVE-01, implement an immutable live-training materialization stage before any fingerprint/reuse/refit decision, following the dated artifact orchestration pattern in `src/cdd_mundial/models/validation.py` and the deterministic JSON/SHA-256/exclusive-write discipline in `src/cdd_mundial/data/provenance.py`. The stage must consume `data/processed/historical_matches.parquet` plus validated played rows from `data/external/results_2026.csv`, map those live rows into the canonical match schema used by `src/cdd_mundial/models/loading.py` without mutating raw/history, and write a dated/versioned processed artifact with provenance metadata. Recompute or refresh the custom Elo/form features chronologically on the combined history + live rows using the existing Phase 2 APIs in `src/cdd_mundial/models/elo.py` and the 90-minute outcome semantics from `src/cdd_mundial/models/loading.py`; keep the conditioned `TournamentState` path separate, but source it from the same validated canonical rows. Only after that artifact exists should the pipeline compute a deterministic fingerprint over the derived live-training artifact plus relevant feature/model parameters, compare it against the latest dated production metadata from `src/cdd_mundial/models/validation.py`, deterministically reuse the pinned artifact when unchanged, and materialize a new dated artifact only when the fingerprint differs.</action>
  <acceptance_criteria>The official run core exposes a pure materialization + selection path that returns the derived live-training artifact path/checksum, refreshed feature metadata, and model provenance. Repeated runs with unchanged canonical results produce the same derived-artifact checksum and input fingerprint and therefore reuse the same dated model artifact/checksum. Newly completed or corrected canonical results produce a changed derived-artifact checksum/fingerprint and therefore trigger exactly one new dated model artifact with `model_version` in the `baseline-v1-YYYY-MM-DD-<shortsha>` shape.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_pipeline.py -x</automated>
  </verify>
  <done>The pipeline can prove that live canonical results first materialize immutable model-facing inputs, then drive reproducible no-op reuse or changed-results refit behavior with no mutation of historical inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simulate and stage the official snapshot only after materialization/refit-reuse resolves</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
    - src/cdd_mundial/data/provenance.py
    - src/cdd_mundial/models/validation.py
    - src/cdd_mundial/live/materialization.py
    - tests/test_provenance.py
  </read_first>
  <files>src/cdd_mundial/live/snapshots.py, src/cdd_mundial/live/pipeline.py, src/cdd_mundial/live/__main__.py, tests/test_live_snapshots.py, tests/test_live_pipeline.py</files>
  <action>Implement the append-only snapshot staging/finalization flow per D-08..D-13, but wire it so simulation and publication cannot begin until the live-training materialization stage and the model reuse/refit decision have completed. Reuse `src/cdd_mundial/models/validation.py` for the fit/materialization orchestration shape and `src/cdd_mundial/data/provenance.py` for deterministic JSON plus SHA-256/exclusive writes. The official command must stage every snapshot component in a temporary sibling directory, write deterministic prediction tables only after the materialized training artifact and model provenance are resolved, defer final metadata until downstream plans have frozen benchmark/report artifacts, compute checksums for all staged files, then publish by atomic rename into the timestamped append-only destination. Expose explicit APIs so later plans can add the frozen benchmark slice, append authoritative ledger rows, register rendered report assets, and finalize metadata exactly once; no published snapshot file may be mutated afterward. The default official path must fail on a dirty worktree per D-11; the CLI may accept `--allow-dirty`, but only to record the override, never to hide it. Add `--verify-only` so the command can validate the materialization -> reuse/refit -> simulation -> publication order and print the intended summary without writing artifacts.</action>
  <acceptance_criteria>`python -m cdd_mundial.live --verify-only` can validate the official run prerequisites and explicitly report the materialization artifact/fingerprint that would feed model selection before simulation. A non-verify official run stages exactly one new snapshot bundle without mutating any published directory, and final metadata is written once, after all staged artifacts are registered, with commit hash, dirty status, live-training artifact provenance, model provenance/fingerprint, and checksums for every required artifact present at publish time.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_snapshots.py tests/test_live_pipeline.py -x</automated>
  </verify>
  <done>The project has a one-command official snapshot path that proves the live-training materialization stage executes before simulation/report consumers and remains reproducible, append-only, and git-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git worktree -> official publication | Local uncommitted changes cross into an artifact that claims auditability |
| Canonical live results -> derived live-training artifact | Manual canonical rows become model-facing training inputs and refreshed features |
| Canonical training inputs -> model artifact selection | The pipeline decides whether to reuse or refit a production model |
| Snapshot staging -> published files | Canonical outputs must be immutable once atomically published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | R | official publication provenance | mitigate | Record commit hash, dirty flag, and relevant modified files in `metadata.json` per D-10/D-11. |
| T-04-05 | T | snapshot publication bundle | mitigate | Stage in a temporary sibling directory, finalize metadata once, then publish only by atomic rename so no file in a published snapshot is mutated afterward per D-09/D-12. |
| T-04-06 | T | live-training materialization and model refresh decision | mitigate | Materialize a dated immutable derived training artifact from historical + validated live rows, hash it with deterministic provenance, then fingerprint that artifact plus feature parameters and store the reuse/refit decision in snapshot metadata per D-06/DOC-02. |
</threat_model>

<verification>
Run the focused pipeline/snapshot tests and confirm the CLI summary remains deterministic under `--verify-only`, including the materialization artifact/fingerprint, while published snapshots remain immutable after atomic finalization.
</verification>

<success_criteria>
`LIVE-01`, `LIVE-02`, and `DOC-02` are structurally reachable because an official command now materializes immutable live-training inputs before reuse/refit, then builds a reproducible snapshot bundle from canonical inputs with explicit provenance and git hygiene.
</success_criteria>

<output>
After completion, create `.planning/phases/04-primer-pronostico-pipeline-diario/04-02-SUMMARY.md`
</output>
