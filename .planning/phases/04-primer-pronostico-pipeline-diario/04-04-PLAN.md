---
phase: 04-primer-pronostico-pipeline-diario
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/cdd_mundial/live/calibration.py
  - tests/test_live_calibration.py
autonomous: true
requirements:
  - LIVE-04
  - DOC-02
decision_ids:
  - D-18
  - D-19
  - D-20
  - D-21
  - D-22
must_haves:
  truths:
    - "La evaluacion en vivo conserva una unidad canonica por partido, no solo agregados por jornada."
    - "El benchmark principal usa la mediana de probabilidades de-margined congelada al publicar el snapshot."
    - "Las vistas acumuladas y series temporales pueden regenerarse desde un ledger append-only autoritativo y slices congelados del snapshot que quedan referenciados en metadata antes de publicar."
  artifacts:
    - path: "src/cdd_mundial/live/calibration.py"
      provides: "Freeze del benchmark y ledger canonico por partido"
    - path: "tests/test_live_calibration.py"
      provides: "Pruebas de mediana/promedio, freeze timing y metricas acumuladas"
  key_links:
    - from: "src/cdd_mundial/live/calibration.py"
      to: "src/cdd_mundial/data/ingest_odds.py"
      via: "semantica de de-margin y linking al fixture"
      pattern: "build_odds_benchmark|demargin_decimal_odds"
    - from: "src/cdd_mundial/live/calibration.py"
      to: "src/cdd_mundial/models/metrics.py"
      via: "RPS/Brier/log-loss acumulados"
      pattern: "rps|brier_multiclass|log_loss"
---

<objective>
Implementar el tracker canonico de calibracion en vivo y el freeze del benchmark de mercado.

Purpose: cerrar `LIVE-04` y `DOC-02` sin duplicar fuentes de verdad, usando un ledger top-level append-only por partido y slices congelados dentro de cada snapshot.
Output: modulo de calibracion/freeze del benchmark, ledger canonico, slices/publication rows para el reporte, y pruebas de metricas acumuladas vs mercado.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
@src/cdd_mundial/data/ingest_odds.py
@src/cdd_mundial/models/metrics.py
@src/cdd_mundial/models/validation.py
@tests/test_odds.py
@tests/test_metrics.py

<interfaces>
This plan must implement the locked research resolution:

```text
Top-level canonical source of truth:
  data/processed/live/calibration/calibration_matches.parquet

Snapshot-local frozen slices:
  snapshot_dir/frozen_benchmark.parquet
  snapshot_dir/upcoming_match_predictions.parquet
  snapshot_dir/report_inputs/calibration_publication_slice.parquet
  snapshot_dir/metadata.json -> checksums/row ids for all publication slices and appended ledger rows
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the market benchmark at publication time</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
    - src/cdd_mundial/data/ingest_odds.py
    - src/cdd_mundial/data/provenance.py
    - tests/test_odds.py
  </read_first>
  <files>src/cdd_mundial/live/calibration.py, tests/test_live_calibration.py</files>
  <action>Implement snapshot-time benchmark freezing per D-19, D-20, and D-21 on top of the `04-02` staging contract. Reuse the exact fixture matching, valid-bookmaker filtering, manual fallback, and `demargin_decimal_odds(...)` semantics from `src/cdd_mundial/data/ingest_odds.py`, then aggregate valid bookmaker rows into a canonical per-match median de-margined probability triplet with simple mean retained as an auxiliary diagnostic. Use the deterministic JSON/SHA-256 and append-only discipline from `src/cdd_mundial/data/provenance.py` when deriving row identifiers/checksums and registering the frozen slice with the staging/finalization API so final metadata can reference it before publication; the evaluation path must never read "latest odds" after snapshot time when scoring a published forecast.</action>
  <acceptance_criteria>Given quote-level benchmark input, the calibration module emits a per-match frozen benchmark slice whose primary benchmark is the median probability vector, whose auxiliary diagnostics preserve the mean, and whose row IDs/checksums are available for one-shot metadata finalization in the published snapshot.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_calibration.py -x</automated>
  </verify>
  <done>Frozen benchmark slices are canonical, snapshot-scoped, and immune to later line movement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the append-only per-match calibration ledger</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
    - src/cdd_mundial/models/metrics.py
    - src/cdd_mundial/models/validation.py
    - tests/test_metrics.py
  </read_first>
  <files>src/cdd_mundial/live/calibration.py, tests/test_live_calibration.py</files>
  <action>Extend the module with the top-level append-only ledger resolved during planning: one canonical row per match/publication pair, storing frozen model probabilities, frozen market benchmark, realized outcome when known, and the derivable inputs needed for cumulative metrics. Per D-18 and D-22, keep jornada views as derived aggregations only; do not create a second mutable summary dataset. During the same publication transaction, derive a snapshot-local `report_inputs/calibration_publication_slice.parquet` from the authoritative rows used by that publication, append only the new ledger rows, and hand both the appended-row IDs and slice checksums back to the snapshot finalizer. Reuse `log_loss` semantics from `src/cdd_mundial/models/validation.py` and `rps(...)` / `brier_multiclass(...)` from `src/cdd_mundial/models/metrics.py` directly so the live-eval math stays consistent with Phase 2 validation.</action>
  <acceptance_criteria>The authoritative ledger appends without mutating existing rows, the publication also writes a snapshot-local calibration slice for report consumption, cumulative model-vs-market metrics and time series are derivable from base per-match records, and metadata can trace the publication back to exact snapshot-local prediction/benchmark slices plus appended ledger rows.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_calibration.py -x</automated>
  </verify>
  <done>The project has one canonical source of truth for live calibration, plus snapshot-local frozen slices for replay and audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quote-level odds inputs -> frozen benchmark | External market data becomes a canonical evaluation baseline |
| Staged snapshot slices -> top-level ledger | Frozen per-publication facts are promoted into long-lived evaluation history before metadata finalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | T | benchmark freeze timing | mitigate | Freeze benchmark rows inside the staged snapshot before final metadata is written and forbid later substitution of fresher odds per D-21. |
| T-04-11 | T | calibration ledger | mitigate | Append only new rows keyed by snapshot/match, validate uniqueness with strict schemas per D-18, and return appended row IDs to the snapshot finalizer. |
| T-04-12 | R | metric comparability | mitigate | Reuse Phase 2 metric helpers and store enough base probabilities/outcomes to recompute cumulative series exactly. |
</threat_model>

<verification>
Run the calibration suite and confirm the median benchmark plus append-only ledger invariants hold.
</verification>

<success_criteria>
`LIVE-04` and the calibration portion of `DOC-02` are complete when a published snapshot can be compared honestly against a frozen multi-bookmaker benchmark from a single canonical per-match ledger.
</success_criteria>

<output>
After completion, create `.planning/phases/04-primer-pronostico-pipeline-diario/04-04-SUMMARY.md`
</output>
