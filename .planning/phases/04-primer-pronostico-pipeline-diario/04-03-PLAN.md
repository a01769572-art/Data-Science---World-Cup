---
phase: 04-primer-pronostico-pipeline-diario
plan: "03"
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-04"
files_modified:
  - pyproject.toml
  - src/cdd_mundial/live/report.py
  - templates/report_base.html.jinja
  - templates/report_daily.html.jinja
  - tests/test_live_report.py
autonomous: true
requirements:
  - LIVE-03
decision_ids:
  - D-18
  - D-22
  - D-12
  - D-14
  - D-15
  - D-16
  - D-17
must_haves:
  truths:
    - "Cada snapshot oficial puede renderizarse a un HTML estatico sin rerun de logica de negocio."
    - "El reporte diario muestra resumen ejecutivo, bloque proximo, probabilidades del torneo, evolucion temporal y nota metodologica."
    - "La comparacion temporal usa el snapshot anterior, el primer snapshot publicado del proyecto y la historia acumulada del ledger canonico de calibracion."
  artifacts:
    - path: "src/cdd_mundial/live/report.py"
      provides: "Renderer snapshot-only a HTML y PNGs"
    - path: "templates/report_daily.html.jinja"
      provides: "Plantilla oficial del reporte diario"
    - path: "tests/test_live_report.py"
      provides: "Pruebas de secciones requeridas y fuente exclusiva en snapshot"
  key_links:
    - from: "src/cdd_mundial/live/report.py"
      to: "templates/report_daily.html.jinja"
      via: "Jinja2 static rendering"
      pattern: "jinja"
    - from: "src/cdd_mundial/live/report.py"
      to: "snapshot_dir"
      via: "lectura exclusiva de parquet/json congelados"
      pattern: "read_parquet|metadata.json"
---

<objective>
Generar el reporte HTML estatico oficial desde artefactos congelados del snapshot.

Purpose: cerrar `LIVE-03` con la ruta mas corta y reproducible hacia un reporte publicable, usando Jinja2 + pandas + Matplotlib/Seaborn como baseline obligatorio.
Output: renderer snapshot-only, plantillas HTML, dependencias declaradas, y pruebas de secciones/series temporales basadas en slices congelados + ledger.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-02-PLAN.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-04-PLAN.md
@src/cdd_mundial/live/pipeline.py
@src/cdd_mundial/live/snapshots.py
@src/cdd_mundial/simulation/outputs.py
@pyproject.toml

<interfaces>
This plan consumes the finalized snapshot contract from `04-02` plus the calibration artifacts fixed by `04-04`, and must not call model/simulation functions directly.

Required snapshot inputs:

```text
snapshot_dir/
  metadata.json
  team_probabilities.parquet
  group_position_probabilities.parquet
  upcoming_match_predictions.parquet
  frozen_benchmark.parquet
  report_inputs/calibration_publication_slice.parquet

Top-level authoritative history:
  data/processed/live/calibration/calibration_matches.parquet
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the static-report interface and dependency declaration</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-RESEARCH.md
    - pyproject.toml
    - src/cdd_mundial/live/snapshots.py
  </read_first>
  <files>pyproject.toml, tests/test_live_report.py</files>
  <action>Declare `Jinja2` as a direct project dependency so a fresh clone can render reports without relying on transitive notebook installs. Write RED tests that assert the renderer consumes only finalized snapshot-local files plus the top-level authoritative calibration ledger, emits the required D-15 sections, produces a mixed KPI + highlighted visual top block per D-16, compares current probabilities against both the immediately previous and first-ever snapshots per D-17, and renders cumulative log-loss/RPS plus a calibration time-series evolution per D-22.</action>
  <acceptance_criteria>`pyproject.toml` declares the baseline report dependency explicitly, and `tests/test_live_report.py` encodes the section-level contract, the snapshot-local-slices-plus-ledger data-source rule, and the D-22 calibration/time-series requirements before implementation starts.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_report.py -x</automated>
  </verify>
  <done>The report contract is explicit, dependency-safe, and ready for implementation on clean environments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the snapshot-only HTML renderer</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-PATTERNS.md
    - src/cdd_mundial/live/pipeline.py
    - src/cdd_mundial/live/snapshots.py
    - src/cdd_mundial/simulation/outputs.py
  </read_first>
  <files>src/cdd_mundial/live/report.py, templates/report_base.html.jinja, templates/report_daily.html.jinja, tests/test_live_report.py</files>
  <action>Implement the baseline report path exactly as resolved in planning: Jinja2 templates, pandas HTML tables, and Matplotlib/Seaborn PNG assets. Per D-12, D-14, D-15, and D-22, the renderer must read only the finalized snapshot-local prediction/benchmark/calibration publication slices plus the authoritative top-level ledger for cumulative history; it must never call model fitting or simulation. Generate executive KPIs, next-block match cards/table, tournament probability visuals, temporal-delta visuals vs previous and first snapshots, cumulative log-loss/RPS comparisons vs the market benchmark, calibration-evolution time series, and a short methodology note. Keep the group-detail section optional and data-driven; Plotly remains optional and must not appear in the critical path.</action>
  <acceptance_criteria>Rendering a finalized snapshot produces `report.html` plus deterministic static image assets inside the snapshot directory; the HTML contains the required sections, temporal comparisons, cumulative model-vs-market metrics, and calibration evolution series; re-rendering the same snapshot yields the same markup structure and does not invoke any upstream business logic.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_report.py -x</automated>
  </verify>
  <done>The project can turn any frozen official snapshot into a publishable static HTML report without recomputing forecasts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot files -> HTML report | Frozen canonical data is transformed into a public-facing artifact |
| Template rendering -> browser | Text/labels cross into HTML output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T | report rendering data source | mitigate | Restrict renderer inputs to finalized snapshot-local slices plus the authoritative calibration ledger and test that no model/simulation calls occur per D-12/D-22. |
| T-04-08 | I | HTML tables | mitigate | Keep pandas HTML escaping enabled by default and avoid injecting raw unsafe HTML into Jinja templates. |
| T-04-09 | R | temporal comparisons | mitigate | Read comparison baselines from first and previous snapshot metadata/files, not from mutable ad hoc state, per D-17. |
</threat_model>

<verification>
Run the report suite and inspect that the renderer contract stays snapshot-local-slices-plus-ledger only.
</verification>

<success_criteria>
`LIVE-03` is complete when the frozen snapshot bundle plus the authoritative calibration ledger are sufficient to render the full official HTML report with deterministic static assets and D-22 cumulative evolution sections.
</success_criteria>

<output>
After completion, create `.planning/phases/04-primer-pronostico-pipeline-diario/04-03-SUMMARY.md`
</output>
