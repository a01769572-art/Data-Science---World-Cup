---
phase: 04-primer-pronostico-pipeline-diario
plan: "05"
type: execute
wave: 5
depends_on:
  - "04-03"
files_modified:
  - src/cdd_mundial/live/materialization.py
  - src/cdd_mundial/live/pipeline.py
  - src/cdd_mundial/live/__main__.py
  - notebooks/04_primer_pronostico_pipeline.ipynb
  - docs/live_pipeline_runbook.md
  - tests/test_live_pipeline.py
  - tests/test_live_reproducibility.py
  - data/processed/live/snapshots/
  - data/processed/live/calibration/
autonomous: false
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03
  - LIVE-04
  - DOC-02
decision_ids:
  - D-02
  - D-07
  - D-10
  - D-11
  - D-12
  - D-13
  - D-15
  - D-17
  - D-21
  - D-22
must_haves:
  truths:
    - "Existe una primera publicacion oficial append-only pre-kickoff con materializacion live-training, snapshot, freeze de benchmark, ledger append y reporte incluidos en un bundle inmutable."
    - "La publicacion oficial falla en worktree sucio por defecto y solo continua con override explicito y registrado."
    - "La corrida oficial puede repetirse desde notebook como interfaz sin redefinir la logica productiva."
  artifacts:
    - path: "notebooks/04_primer_pronostico_pipeline.ipynb"
      provides: "Notebook interfaz que dispara e inspecciona la corrida oficial"
    - path: "docs/live_pipeline_runbook.md"
      provides: "Runbook operador para publicacion diaria y manejo de fallos"
    - path: "data/processed/live/snapshots/"
      provides: "Primer snapshot oficial y reporte HTML asociado"
  key_links:
    - from: "src/cdd_mundial/live/__main__.py"
      to: "src/cdd_mundial/live/materialization.py"
      via: "materializacion derivada y fingerprint antes de simular o reportar"
      pattern: "materialize"
    - from: "src/cdd_mundial/live/__main__.py"
      to: "src/cdd_mundial/live/report.py"
      via: "integracion de render oficial"
      pattern: "report"
    - from: "src/cdd_mundial/live/__main__.py"
      to: "src/cdd_mundial/live/calibration.py"
      via: "freeze de benchmark y append al ledger"
      pattern: "calibration"
    - from: "notebooks/04_primer_pronostico_pipeline.ipynb"
      to: "python -m cdd_mundial.live"
      via: "notebook-as-interface, no business logic duplicada"
      pattern: "cdd_mundial.live"
---

<objective>
Integrar reporte + calibracion en la CLI oficial, publicar el primer snapshot oficial y dejar el runbook/notebook de operacion diaria.

Purpose: cerrar la fase con un artefacto real de publicacion antes del 2026-06-27, no solo con infraestructura reusable.
Output: comando oficial end-to-end, notebook interfaz, runbook operativo y primera publicacion oficial append-only con evidencia reproducible.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-03-PLAN.md
@.planning/phases/04-primer-pronostico-pipeline-diario/04-04-PLAN.md
@.planning/phases/03-simulador-del-torneo/03-05-SUMMARY.md
@src/cdd_mundial/live/pipeline.py
@src/cdd_mundial/live/__main__.py
@src/cdd_mundial/live/materialization.py
@src/cdd_mundial/live/report.py
@src/cdd_mundial/live/calibration.py
@notebooks/03_simulador_torneo.ipynb
@README.md

<interfaces>
Expected official command shape after wiring:

```text
.\.venv\python.exe -m cdd_mundial.live --official --results-csv data/external/results_2026.csv --seed <int> [--manual-odds <csv>] [--allow-dirty]
```

The notebook is an interface over this command/module, not a second implementation path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire report and calibration into the official command with reproducibility tests</name>
  <read_first>
    - src/cdd_mundial/live/materialization.py
    - src/cdd_mundial/live/pipeline.py
    - src/cdd_mundial/live/__main__.py
    - src/cdd_mundial/live/report.py
    - src/cdd_mundial/live/calibration.py
    - tests/test_live_pipeline.py
  </read_first>
  <files>src/cdd_mundial/live/materialization.py, src/cdd_mundial/live/pipeline.py, src/cdd_mundial/live/__main__.py, tests/test_live_pipeline.py, tests/test_live_reproducibility.py</files>
  <action>Integrate the `04-03` renderer and `04-04` calibration ledger into the official CLI path so one command performs, in this exact order: results validation -> immutable live-training materialization -> model fingerprint + reuse/refit decision -> conditioned simulation -> snapshot staging -> frozen benchmark -> authoritative ledger append + snapshot-local calibration slice -> report render/assets -> checksum aggregation -> one-shot metadata finalization -> atomic publish. Reuse the fit/materialization orchestration shape from `src/cdd_mundial/models/validation.py`, the provenance/write rules from `src/cdd_mundial/data/provenance.py`, the odds-freeze semantics from `src/cdd_mundial/data/ingest_odds.py`, and the log-loss/RPS semantics from `src/cdd_mundial/models/validation.py` plus `src/cdd_mundial/models/metrics.py`. Preserve the invariant that no file inside a published snapshot is mutated afterward. Add end-to-end and reproducibility tests that assert the same canonical inputs/seed produce identical materialized live-training artifact fingerprints, identical critical snapshot tables, stable row IDs/checksums for deterministic slices, stable metadata fields except for allowed timestamps, and a safe non-writing `--verify-only` path.</action>
  <acceptance_criteria>The official command now covers the full publication chain with atomic finalization, and focused integration tests prove the materialization stage executes before simulation/reporting, deterministic no-op reuse when results are unchanged, deterministic refit/new artifact when canonical results change, verify-only behavior, and no post-publication mutation. The integration does not introduce a second business-logic path outside `src/`.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_pipeline.py tests/test_live_reproducibility.py -x</automated>
  </verify>
  <done>The code path for the first official publication is end-to-end complete and reproducible.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Resolve the dirty-worktree publication gate if it triggers</name>
  <read_first>
    - .planning/phases/04-primer-pronostico-pipeline-diario/04-CONTEXT.md
    - .planning/STATE.md
  </read_first>
  <files>None</files>
  <action>Run this checkpoint only if the official publication command fails on the default git-clean gate. Per D-11, do not bypass the failure automatically. Present the actual dirty file list and ask the user to choose between cleaning the worktree first or explicitly approving one dirty publication with recorded metadata via `--allow-dirty`.</action>
  <acceptance_criteria>No official publication proceeds past a dirty-worktree failure unless the user explicitly authorizes the override and understands that `metadata.json` will record `dirty=true` and the relevant modified files.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m cdd_mundial.live --official --results-csv data/external/results_2026.csv --manual-odds data/external/odds_2026_template.csv --seed 20260613 --verify-only</automated>
  </verify>
  <done>The publication gate remains fail-closed by default, with an explicit human decision only when required.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the first official snapshot and document the daily operator interface</name>
  <read_first>
    - notebooks/03_simulador_torneo.ipynb
    - README.md
    - src/cdd_mundial/live/__main__.py
    - docs/live_pipeline_runbook.md
  </read_first>
  <files>notebooks/04_primer_pronostico_pipeline.ipynb, docs/live_pipeline_runbook.md, data/processed/live/snapshots/, data/processed/live/calibration/</files>
  <action>Create the Phase 4 notebook-as-interface that invokes the official pipeline and inspects the returned artifact paths without redefining production behavior. Add a concise operator runbook documenting the exact commands, required preflight checks, failure modes (missing results, live-training materialization drift, odds/manual benchmark fallback, dirty worktree, snapshot append-only collision), and publication checklist. Then execute one real official publication using the current canonical inputs and manual-odds fallback when no provider key is configured, producing the first append-only snapshot directory, immutable live-training artifact provenance, frozen prediction and benchmark slices, snapshot-local calibration publication slice, authoritative ledger append, and rendered `report.html` plus assets. The published `metadata.json` must record the UTC publication timestamp, kickoff boundary, git commit, dirty status, live-training artifact path/checksum, model provenance including input fingerprint and reuse/refit decision, seed, preflight outcome, publication row IDs, and artifact checksums. If the worktree is clean, commit the publication artifacts before kickoff; if the user approved `--allow-dirty`, record that in metadata and in the runbook note for this publication.</action>
  <acceptance_criteria>A first official snapshot directory exists under the live snapshots root with immutable metadata containing UTC timestamp, kickoff boundary, commit, dirty status, live-training artifact provenance, model provenance/fingerprint, seed, preflight outcome, publication row IDs, and checksums; the required frozen tables/slices, authoritative ledger effect, and rendered HTML report/assets all exist; the notebook can reproduce the operator flow as an interface; the runbook contains exact repo-local `.\.venv\python.exe` commands and fail-closed publication rules.</acceptance_criteria>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q tests/test_live_pipeline.py tests/test_live_reproducibility.py -x</automated>
    <automated>.\.venv\python.exe -m cdd_mundial.live --official --results-csv data/external/results_2026.csv --manual-odds data/external/odds_2026_template.csv --seed 20260613</automated>
    <automated>PowerShell -NoProfile -Command "$latest = Get-ChildItem 'data/processed/live/snapshots' -Directory | Sort-Object Name | Select-Object -Last 1; if (-not $latest) { throw 'No snapshot directory created.' }; $meta = Get-Content ($latest.FullName + '\\metadata.json') | ConvertFrom-Json; if (-not $meta.generated_at_utc) { throw 'Missing generated_at_utc.' }; if (-not $meta.kickoff_boundary_utc) { throw 'Missing kickoff_boundary_utc.' }; if (-not $meta.git_commit) { throw 'Missing git_commit.' }; if ($null -eq $meta.git_dirty) { throw 'Missing git_dirty.' }; if (-not $meta.live_training_provenance.artifact_path) { throw 'Missing live-training artifact path.' }; if (-not $meta.live_training_provenance.artifact_sha256) { throw 'Missing live-training artifact SHA-256.' }; if (-not $meta.model_provenance.input_fingerprint) { throw 'Missing model fingerprint.' }; if (-not $meta.model_provenance.artifact_sha256) { throw 'Missing model artifact SHA-256.' }; if (-not $meta.seed) { throw 'Missing seed.' }; if (-not $meta.preflight) { throw 'Missing preflight outcome.' }; if (-not $meta.checksums) { throw 'Missing checksums.' }; $required = @('team_probabilities.parquet','upcoming_match_predictions.parquet','frozen_benchmark.parquet','report.html'); foreach ($name in $required) { if (-not (Test-Path (Join-Path $latest.FullName $name))) { throw ('Missing ' + $name + '.'); } }; if (-not (Test-Path (Join-Path $latest.FullName 'report_inputs\\calibration_publication_slice.parquet'))) { throw 'Missing calibration publication slice.' }; if (-not (Test-Path (Join-Path $latest.FullName 'assets'))) { throw 'Missing report assets directory.' }; $ledger = 'data/processed/live/calibration/calibration_matches.parquet'; if (-not (Test-Path $ledger)) { throw 'Missing calibration ledger.' }; if (-not $meta.publication_row_ids -or $meta.publication_row_ids.Count -lt 1) { throw 'Metadata missing publication_row_ids.' }; $dupes = .\\.venv\\python.exe -c \"import pandas as pd; df = pd.read_parquet(r'data/processed/live/calibration/calibration_matches.parquet'); print(int(df.duplicated(subset=['snapshot_id','match_id']).sum()))\"; if ($LASTEXITCODE -ne 0) { throw 'Ledger duplicate check failed.' }; if ([int](($dupes | Out-String).Trim()) -ne 0) { throw 'Ledger contains duplicate snapshot/match rows.' }; if ($meta.generated_at_utc -ge $meta.kickoff_boundary_utc) { throw 'Snapshot recorded after kickoff boundary.' }; $commitIso = git log -1 --format=%cI -- $latest.FullName; if (-not $commitIso) { throw 'Snapshot directory is not committed.' }; if ([datetimeoffset]::Parse($commitIso) -ge [datetimeoffset]::Parse($meta.kickoff_boundary_utc)) { throw 'Snapshot commit is not before kickoff boundary.' }"</automated>
  </verify>
  <done>Phase 4 has shipped a real first official publication artifact plus the documented daily operating path required to keep publishing through the group stage, and the verification proves the immutable snapshot bundle, metadata fields, ledger append, report artifacts, and pre-kickoff publication boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official CLI -> published artifacts | The final public forecast is emitted from local canonical inputs and repo state |
| Notebook interface -> production code | Human-facing exploration must not become an alternate source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | R | first official publication | mitigate | Enforce the dirty-worktree gate by default and record explicit overrides in metadata/runbook per D-11. |
| T-04-14 | T | notebook interface | mitigate | Keep notebook as a thin caller of `python -m cdd_mundial.live` or imported pipeline functions, never a separate implementation per D-07. |
| T-04-15 | R | reproducibility of first publish | mitigate | Verify deterministic reruns with fixed seeds, assert the immutable publication bundle fields, and commit or explicitly record the publication state before the recorded kickoff per LIVE-02/DOC-02. |
</threat_model>

<verification>
Run the focused end-to-end and reproducibility suites, then execute the official publication command in the chosen clean or approved-dirty mode and confirm the live-training materialization artifact is recorded before simulation/report outputs.
</verification>

<success_criteria>
Phase 4 is done when a real first official pre-kickoff publication exists, is append-only and reproducible, proves live-training materialization before reuse/refit and simulation/reporting, and leaves a documented daily path to keep producing subsequent snapshots.
</success_criteria>

<output>
After completion, create `.planning/phases/04-primer-pronostico-pipeline-diario/04-05-SUMMARY.md`
</output>
