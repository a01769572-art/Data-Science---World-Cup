---
phase: 01-fundaci-n-de-datos
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - src/cdd_mundial/data/ingest_odds.py
  - data/external/odds_2026_template.csv
  - data/metadata/odds_provider_policy.json
  - tests/fixtures/odds/sports.json
  - tests/fixtures/odds/odds.json
  - tests/test_odds.py
autonomous: false
requirements:
  - DATA-02
  - DATA-05
  - DOC-03
user_setup:
  - service: odds-provider
    why: "The selected live odds source may require a personal API key and acceptance of provider terms."
    env_vars:
      - name: ODDS_API_KEY
        source: "Selected provider account/dashboard after the capability and terms review"
    local_dev:
      - "Set ODDS_API_KEY in the local `.env`; never place the value in `.env.example`, notebooks, logs, or committed metadata."
must_haves:
  truths:
    - "Provider selection is based on a recorded capability and terms probe, not a hard-coded assumption."
    - "Only three-way 90-minute soccer prices are accepted for the benchmark."
    - "Raw and de-margined probabilities are linked to canonical fixture match IDs and sum correctly."
    - "Secrets and restricted raw odds payloads never enter the public repository."
  artifacts:
    - path: "src/cdd_mundial/data/ingest_odds.py"
      provides: "Provider capability probe, odds normalization, and de-margining"
      exports: ["probe_odds_provider", "demargin_decimal_odds", "build_odds_benchmark"]
    - path: "data/external/odds_2026_template.csv"
      provides: "Manual import fallback with the canonical odds contract"
      contains: "provider,bookmaker,event_id,match_id"
    - path: "data/metadata/odds_provider_policy.json"
      provides: "Recorded provider, terms URL, storage policy, and probe timestamp"
    - path: "tests/test_odds.py"
      provides: "Three-way market and probability correctness gates"
  key_links:
    - from: "src/cdd_mundial/data/ingest_odds.py"
      to: "data/external/fixture_2026.csv"
      via: "event-to-match resolution before benchmark output"
      pattern: "match_id"
    - from: "src/cdd_mundial/data/ingest_odds.py"
      to: "src/cdd_mundial/data/contracts.py"
      via: "OddsSchema validation"
      pattern: "OddsSchema"
---

<objective>
Select and integrate one legally reviewed World Cup odds source and produce canonical de-margined three-way benchmark probabilities.

Purpose: Market probabilities are the academic benchmark for later model evaluation; provider availability and licensing must be proven rather than assumed.
Output: Capability probe, provider policy metadata, canonical odds adapter/manual fallback, and validated benchmark snapshot.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md
@.planning/phases/01-fundaci-n-de-datos/01-03-SUMMARY.md
@data/external/teams.csv
@data/external/team_aliases.csv
@data/external/fixture_2026.csv
@src/cdd_mundial/data/http.py
@src/cdd_mundial/data/contracts.py
@src/cdd_mundial/data/identities.py
</context>

<threat_model>
T-01-01 - Secret/license exposure: provider keys or restricted raw responses could be committed. Mitigate with environment-only keys, redacted errors, ignored raw odds paths, and an explicit provider storage policy.

T-01-07 - Market semantic mismatch: accepting a two-way market, outright, extra-time-inclusive quote, stale event, or unmatched team could create invalid benchmark probabilities. Mitigate with exact three-outcome validation, market/time metadata, canonical fixture linkage, freshness fields, and explicit rejection.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Probe provider capability and record the storage policy</name>
  <files>src/cdd_mundial/data/ingest_odds.py, data/metadata/odds_provider_policy.json, tests/fixtures/odds/sports.json, tests/test_odds.py</files>
  <read_first>.planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md, .env.example, .gitignore, src/cdd_mundial/data/http.py, data/external/fixture_2026.csv</read_first>
  <action>Implement `probe_odds_provider` with The Odds API v4 as the first candidate: query `/v4/sports` using `ODDS_API_KEY`, locate the active World Cup 2026 soccer sport key rather than hard-coding one, and verify that event odds can return `h2h` with home/draw/away outcomes. If the candidate is unavailable or terms/storage are unsuitable, test a documented alternative or select the authored CSV import fallback. Write `data/metadata/odds_provider_policy.json` with `provider`, `sports_endpoint`, `sport_key`, `terms_url`, `license_or_terms_summary`, `raw_storage` (`ignored` or `committed`), `derived_storage`, `probed_at_utc`, and `decision`. Redact the API key from exceptions and logs. Mock the sports response in tests.</action>
  <verify>python -m pytest -q tests/test_odds.py -k "probe or secret or policy"</verify>
  <acceptance_criteria>
    - `probe_odds_provider` reads `ODDS_API_KEY` from the environment and never accepts it as persisted metadata
    - Provider policy JSON contains all nine required fields and a non-empty `decision`
    - Unsupported competition or missing three-way market raises `SourceUnavailableError`
    - Tests assert the literal test API key does not appear in exception text or generated files
    - Targeted pytest command exits 0
  </acceptance_criteria>
  <done>A provider/fallback and repository storage policy are selected with auditable evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Complete the selected odds provider credential or account-consent gate.</action>
  <instructions>The capability probe and provider-policy artifact are complete. Review the selected provider terms and obtain the credential if the API requires one. Provide the key through the secure credential prompt so the executor can write it to the ignored local `.env`; never place it in source files, notebooks, committed metadata, or normal chat text.</instructions>
  <verification>The executor reruns the provider sports/odds probe, confirms authentication succeeds, and verifies that logs and generated metadata contain no key value.</verification>
  <resume-signal>Type "done" after the terms review and local key setup, or describe the provider blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Normalize three-way prices and build the benchmark snapshot</name>
  <files>src/cdd_mundial/data/ingest_odds.py, data/external/odds_2026_template.csv, tests/fixtures/odds/odds.json, tests/test_odds.py</files>
  <read_first>data/metadata/odds_provider_policy.json, data/external/fixture_2026.csv, data/external/team_aliases.csv, src/cdd_mundial/data/contracts.py, src/cdd_mundial/data/identities.py, src/cdd_mundial/data/provenance.py</read_first>
  <action>Implement `demargin_decimal_odds(prices)` using multiplicative normalization of inverse decimal odds and reject non-finite or `<=1.0` prices. Implement `build_odds_benchmark` that accepts provider JSON or the canonical manual CSV template, requires market `h2h`, exactly three outcomes matching home/draw/away, resolves provider team names through source `odds`, links events to `fixture_2026.csv` by canonical teams plus commence-time tolerance, stores `captured_at_utc` and provider update time, computes raw implied probabilities, overround, and normalized probabilities, validates with `OddsSchema`, and writes `data/processed/odds_2026.parquet`. Store raw provider payload only at the policy-approved path; default to ignored `data/raw/odds/`. Include tests for valid three-way markets, two-way rejection, duplicate draw outcomes, unmatched fixtures, stale timestamps, and probability sums.</action>
  <verify>python -m pytest -q tests/test_odds.py</verify>
  <acceptance_criteria>
    - `demargin_decimal_odds([2.0, 3.0, 4.0])` returns three probabilities summing to 1 within `1e-9`
    - Two-outcome or non-`h2h` market fixtures raise a validation error
    - Every output row has a non-null canonical `match_id`, `home_team_id`, and `away_team_id`
    - Output stores raw probabilities, `overround`, and de-margined probabilities
    - `OddsSchema.validate` runs before parquet serialization
    - `python -m pytest -q tests/test_odds.py` exits 0
  </acceptance_criteria>
  <done>A real or manually imported source produces valid market benchmark probabilities without leaking credentials or restricted data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_odds.py`
- [ ] `python -m ruff check src/cdd_mundial/data/ingest_odds.py tests/test_odds.py`
- [ ] `data/metadata/odds_provider_policy.json` records current terms and storage policy
- [ ] At least one current World Cup fixture has a validated three-way benchmark row
- [ ] No API key or restricted raw payload is tracked
</verification>

<success_criteria>
- Odds source capability and legal storage policy are documented.
- Three-way prices resolve to canonical fixture IDs.
- De-margined probabilities are reproducible and sum to one.
- Provider failure cannot silently produce fake or semantically incompatible benchmark data.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-de-datos/01-04-SUMMARY.md`
</output>
