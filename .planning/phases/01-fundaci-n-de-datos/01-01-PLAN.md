---
phase: 01-fundaci-n-de-datos
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .env.example
  - src/cdd_mundial/__init__.py
  - src/cdd_mundial/data/__init__.py
  - src/cdd_mundial/data/contracts.py
  - src/cdd_mundial/data/provenance.py
  - tests/conftest.py
  - tests/test_contracts.py
  - tests/test_repository.py
  - tests/test_provenance.py
autonomous: true
requirements:
  - DATA-01
  - DOC-03
user_setup: []
must_haves:
  truths:
    - "The repository installs as a Python package with the pinned tournament-safe data stack."
    - "Every acquired file can be described by a provenance manifest containing source, retrieval time, version, checksum, and license."
    - "Secrets and restricted source captures are excluded from version control by default."
  artifacts:
    - path: "pyproject.toml"
      provides: "Package, dependency, pytest, and ruff configuration"
      contains: "pandas~=2.3.3"
    - path: "src/cdd_mundial/data/contracts.py"
      provides: "Shared pandera schemas for canonical phase outputs"
      contains: "import pandera.pandas as pa"
    - path: "src/cdd_mundial/data/provenance.py"
      provides: "Checksums and metadata manifest utilities"
      exports: ["file_sha256", "write_provenance_manifest"]
    - path: "tests/test_repository.py"
      provides: "Public-repository safety checks"
  key_links:
    - from: "src/cdd_mundial/data/provenance.py"
      to: "data/metadata"
      via: "JSON manifest writer"
      pattern: "data/metadata"
    - from: "pyproject.toml"
      to: "tests"
      via: "pytest configuration"
      pattern: "pytest"
---

<objective>
Create the project scaffold, dependency contract, reusable dataframe schemas, and provenance/security foundation.

Purpose: Every later ingestion plan needs a stable package layout, strict contracts, fast tests, immutable-capture metadata, and public-repository safeguards.
Output: Installable Python package, core data contracts, provenance helpers, and Wave 0 test infrastructure.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md
@.planning/phases/01-fundaci-n-de-datos/01-VALIDATION.md
</context>

<threat_model>
T-01-01 - Secret or licensed-data exposure: API keys, `.env`, notebook credentials, and provider payloads with unknown redistribution rights could enter the public repository. Mitigate with deny-by-default `.gitignore`, `.env.example` containing names only, repository tests, and license metadata on every capture.

T-01-02 - Raw-data mutation or provenance loss: source files could be overwritten or processed in place, making published outputs irreproducible. Mitigate with immutable capture paths, SHA-256 manifests, and explicit refusal to overwrite an existing capture with a different checksum.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the installable package and test scaffold</name>
  <files>pyproject.toml, .gitignore, .env.example, src/cdd_mundial/__init__.py, src/cdd_mundial/data/__init__.py, tests/conftest.py, tests/test_repository.py</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md, .planning/phases/01-fundaci-n-de-datos/01-VALIDATION.md</read_first>
  <action>Create a `src`-layout package named `cdd-mundial` requiring Python `>=3.11,<3.13`. In `pyproject.toml`, declare runtime dependencies `pandas~=2.3.3`, `numpy>=2,<2.5`, `pyarrow>=18`, `pandera[pandas]~=0.31`, `kagglehub>=1,<2`, `requests>=2.32,<3`, and `python-dotenv>=1,<2`; declare development dependencies `pytest>=8,<9`, `ruff>=0.11,<1`, `jupyterlab>=4,<5`, `ipykernel>=6,<7`, and `nbformat>=5,<6`. Configure pytest with `testpaths=["tests"]`, markers `network` and `manual`, and default `-ra`; configure ruff for Python 3.11 and line length 100. Add `.gitignore` entries for `.env`, `.venv/`, caches, notebook checkpoints, `data/raw/restricted/`, `data/raw/odds/`, generated `data/processed/`, and model artifacts while allowing authored CSV reference files and metadata manifests. Add `.env.example` with `ODDS_API_KEY=` only. Create repository tests that assert these exact protections and dependency pins. Install the editable development environment with `python -m pip install -e ".[dev]"` before running pytest.</action>
  <verify>python -m pytest -q tests/test_repository.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.11,<3.13"` and `pandas~=2.3.3`
    - `pyproject.toml` defines pytest markers named `network` and `manual`
    - `.gitignore` contains `.env`, `data/raw/restricted/`, `data/raw/odds/`, and `data/processed/`
    - `.env.example` contains `ODDS_API_KEY=` and no non-empty secret value
    - `python -m pytest -q tests/test_repository.py` exits 0
  </acceptance_criteria>
  <done>The package scaffold installs, the test harness runs, and repository safety rules are executable.</done>
</task>

<task type="auto">
  <name>Task 2: Define strict canonical dataframe contracts</name>
  <files>src/cdd_mundial/data/contracts.py, tests/test_contracts.py</files>
  <read_first>pyproject.toml, .planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md</read_first>
  <action>Implement pandera `DataFrameModel` classes using `import pandera.pandas as pa` for `TeamsSchema`, `TeamAliasesSchema`, `HistoricalMatchesSchema`, `EloRatingsSchema`, `FixtureSchema`, and `OddsSchema`. Use `Config.strict = True` and `Config.coerce = True` for canonical outputs. Include dataframe checks for distinct home/away IDs, nonnegative scores, `neutral` boolean, 2026 group values A-L, timezone-aware kickoff strings ending in `Z`, positive decimal odds, nonnegative overround, and de-margined probabilities summing to 1 within `1e-9`. Keep source-specific parser schemas separate from canonical schemas so raw columns are not silently accepted downstream. Add dedicated tests that prove valid minimal frames pass and unexpected columns, negative scores, self-matches, invalid groups, non-UTC kickoffs, invalid prices, and non-normalized probabilities fail.</action>
  <verify>python -m pytest -q tests/test_contracts.py</verify>
  <acceptance_criteria>
    - `contracts.py` contains classes `TeamsSchema`, `TeamAliasesSchema`, `HistoricalMatchesSchema`, `EloRatingsSchema`, `FixtureSchema`, and `OddsSchema`
    - `contracts.py` contains `import pandera.pandas as pa`
    - Every canonical schema has `strict = True` and `coerce = True`
    - `OddsSchema` checks that `prob_home + prob_draw + prob_away` equals 1 within tolerance
    - `python -m pytest -q tests/test_contracts.py` exits 0
  </acceptance_criteria>
  <done>All Phase 1 outputs have reusable strict runtime contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement immutable provenance manifests</name>
  <files>src/cdd_mundial/data/provenance.py, tests/test_provenance.py</files>
  <read_first>src/cdd_mundial/data/contracts.py, tests/conftest.py, .gitignore</read_first>
  <action>Implement `file_sha256(path: Path) -> str`, a serializable `ProvenanceRecord` with fields `source`, `source_url`, `retrieved_at_utc`, `source_version`, `sha256`, `license`, `local_path`, and optional `notes`, plus `write_provenance_manifest(record, metadata_root=Path("data/metadata")) -> Path`. Write deterministic UTF-8 JSON with sorted keys and ISO-8601 UTC timestamps. Add `copy_immutable_capture(source_path, destination_path)` that creates parent directories, copies once, and raises `FileExistsError` when an existing destination checksum differs; an identical existing file is accepted without rewriting. Test checksum stability, manifest content, identical replay, and overwrite rejection using temporary directories.</action>
  <verify>python -m pytest -q tests/test_provenance.py</verify>
  <acceptance_criteria>
    - `provenance.py` exports `file_sha256`, `ProvenanceRecord`, `write_provenance_manifest`, and `copy_immutable_capture`
    - Manifest JSON includes all eight required provenance fields
    - A different payload targeting an existing immutable path raises `FileExistsError`
    - `python -m pytest -q tests/test_provenance.py` exits 0
  </acceptance_criteria>
  <done>Every source capture can be checksummed, described, and protected against mutation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_repository.py tests/test_contracts.py tests/test_provenance.py`
- [ ] `python -m ruff check src tests`
- [ ] `python -c "import pandas, pyarrow, pandera, kagglehub, requests; print('stack-ok')"` prints `stack-ok`
- [ ] No secret value or raw provider payload is tracked
</verification>

<success_criteria>
- All tasks completed and tests pass.
- Package versions and Python range match the research contract.
- Canonical schemas reject unexpected columns and invalid invariants.
- Immutable capture behavior and provenance metadata are automated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-de-datos/01-01-SUMMARY.md`
</output>
