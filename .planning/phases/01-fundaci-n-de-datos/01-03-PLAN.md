---
phase: 01-fundaci-n-de-datos
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/cdd_mundial/data/http.py
  - src/cdd_mundial/data/ingest_elo.py
  - src/cdd_mundial/data/ingest_fixture.py
  - data/external/fixture_2026.csv
  - tests/fixtures/eloratings/World.tsv
  - tests/fixtures/eloratings/en.teams.tsv
  - tests/fixtures/fixture/fixture_2026_sample.csv
  - tests/test_ingest_elo.py
  - tests/test_fixture.py
autonomous: false
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
user_setup:
  - service: fifa
    why: "The final 104-row fixture requires a human cross-check against FIFA's current authoritative schedule."
    dashboard_config:
      - task: "Open the official FIFA World Cup 26 schedule artifact"
        location: "FIFA tournament schedule page or official downloadable schedule"
        details: "Compare match IDs/slots, kickoff UTC, groups, venues, and host cities against data/external/fixture_2026.csv."
must_haves:
  truths:
    - "Current Elo ratings and Elo name aliases are fetched from the documented TSV endpoints with bounded HTTP behavior and provenance."
    - "All 48 participants resolve to canonical IDs in the Elo snapshot."
    - "The frozen fixture contains exactly 104 unique matches and 72 group-stage matches with UTC kickoffs."
    - "The fixture has been cross-checked against the current official FIFA schedule."
  artifacts:
    - path: "src/cdd_mundial/data/http.py"
      provides: "Timeout, retry, user-agent, and response validation policy"
      exports: ["fetch_bytes"]
    - path: "src/cdd_mundial/data/ingest_elo.py"
      provides: "Elo TSV acquisition and canonical snapshot"
      exports: ["fetch_elo_snapshot"]
    - path: "data/external/fixture_2026.csv"
      provides: "Reviewed tournament fixture"
      contains: "match_id,stage,group,home_slot,away_slot"
    - path: "src/cdd_mundial/data/ingest_fixture.py"
      provides: "Fixture import, normalization, and validation"
      exports: ["load_fixture_2026", "validate_fixture_structure"]
  key_links:
    - from: "src/cdd_mundial/data/ingest_elo.py"
      to: "https://www.eloratings.net/World.tsv"
      via: "fetch_bytes"
      pattern: "World\\.tsv"
    - from: "src/cdd_mundial/data/ingest_fixture.py"
      to: "data/external/team_aliases.csv"
      via: "fixture source identity resolution"
      pattern: "TeamResolver"
---

<objective>
Add current Elo ratings and freeze the authoritative 2026 fixture as validated canonical reference datasets.

Purpose: Phase 2 needs current strengths, while Phases 3 and 4 need a stable 104-match schedule with canonical identities and UTC times.
Output: Safe HTTP helper, Elo snapshot adapter, reviewed fixture CSV/importer, provenance, and contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md
@.planning/phases/01-fundaci-n-de-datos/01-02-SUMMARY.md
@data/external/teams.csv
@data/external/team_aliases.csv
@src/cdd_mundial/data/contracts.py
@src/cdd_mundial/data/identities.py
@src/cdd_mundial/data/provenance.py
</context>

<threat_model>
T-01-05 - External HTTP failure/abuse: unbounded requests, HTML error pages parsed as data, or repeated aggressive access can break the pipeline or burden providers. Mitigate with explicit connect/read timeouts, limited retries with backoff, User-Agent, content checks, and immutable cache-first captures.

T-01-06 - Fixture integrity/timezone error: wrong match IDs, local times, duplicate rows, or stale venue assignments would corrupt simulation state. Mitigate with structural invariants, UTC normalization, source/checksum metadata, and a blocking human cross-check against FIFA.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded HTTP acquisition and Elo TSV ingestion</name>
  <files>src/cdd_mundial/data/http.py, src/cdd_mundial/data/ingest_elo.py, tests/fixtures/eloratings/World.tsv, tests/fixtures/eloratings/en.teams.tsv, tests/test_ingest_elo.py</files>
  <read_first>src/cdd_mundial/data/provenance.py, src/cdd_mundial/data/identities.py, src/cdd_mundial/data/contracts.py, data/external/team_aliases.csv, .planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md</read_first>
  <action>Implement `fetch_bytes(url, *, connect_timeout=5, read_timeout=20, retries=2, backoff_seconds=1.0, user_agent="CDD-MUNDIAL/1.0 (+public research project)")` using `requests.Session`. Retry only connection errors, timeouts, and status 429/5xx; call `raise_for_status`; reject empty bodies and HTML content for TSV endpoints. Implement `fetch_elo_snapshot` for `https://www.eloratings.net/World.tsv` and `https://www.eloratings.net/en.teams.tsv`, capture both immutably with provenance, parse using tab separators, map Elo code/name through source `eloratings`, validate numeric ratings and unique canonical IDs, and write `data/processed/elo_current.parquet`. Add local fixtures and mock HTTP tests for success, timeout, 500 retry, HTML rejection, alias mapping, and schema failure.</action>
  <verify>python -m pytest -q tests/test_ingest_elo.py</verify>
  <acceptance_criteria>
    - `http.py` defaults to connect timeout 5, read timeout 20, retries 2, and the `CDD-MUNDIAL/1.0` User-Agent
    - `ingest_elo.py` contains exact URLs ending in `World.tsv` and `en.teams.tsv`
    - Elo output contains one canonical row per mapped team with numeric rating
    - The 48-participant coverage assertion reports no unresolved Elo identities
    - `python -m pytest -q tests/test_ingest_elo.py` exits 0 without network access
  </acceptance_criteria>
  <done>Current Elo data is safely acquired, canonically mapped, cached, and validated.</done>
</task>

<task type="auto">
  <name>Task 2: Build and structurally validate the frozen 2026 fixture</name>
  <files>data/external/fixture_2026.csv, src/cdd_mundial/data/ingest_fixture.py, tests/fixtures/fixture/fixture_2026_sample.csv, tests/test_fixture.py</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-fundaci-n-de-datos/01-RESEARCH.md, data/external/teams.csv, data/external/team_aliases.csv, src/cdd_mundial/data/contracts.py, src/cdd_mundial/data/identities.py</read_first>
  <action>Create `fixture_2026.csv` from the current official FIFA schedule using columns `match_id,stage,group,home_slot,away_slot,home_team_id,away_team_id,kickoff_utc,venue,host_city,host_country,status,source_url,verified_at_utc`. Use stable official match numbers/slots; preserve unresolved knockout participants in `home_slot`/`away_slot` with nullable team IDs; normalize kickoff timestamps to ISO-8601 UTC ending `Z`; resolve named participants through source `fixture`. Implement `load_fixture_2026` and `validate_fixture_structure` enforcing 104 unique match IDs, 72 group-stage matches, exactly six matches per group A-L, no duplicate scheduled pairing IDs, allowed stages, host countries `CAN`, `MEX`, `USA`, and non-null team IDs for all group-stage participants. Validate with `FixtureSchema` and retain the official source URL and verification timestamp on every row.</action>
  <verify>python -m pytest -q tests/test_fixture.py</verify>
  <acceptance_criteria>
    - `fixture_2026.csv` contains exactly 104 data rows and unique `match_id`
    - Exactly 72 rows have `stage=group` and each group A-L has six rows
    - Every `kickoff_utc` value ends with `Z`
    - Group-stage rows have two non-null canonical team IDs
    - Knockout rows retain slot references even when team IDs are null
    - `python -m pytest -q tests/test_fixture.py` exits 0
  </acceptance_criteria>
  <done>The complete fixture is a stable, canonical, schema-validated artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Current Elo snapshot pipeline and `data/external/fixture_2026.csv` with 104 validated matches.</what-built>
  <how-to-verify>Open the current official FIFA World Cup 26 schedule. Compare the CSV's 104 match numbers/slots, group assignments, kickoff times converted to UTC, venues, and host cities. Confirm that all 48 group-stage teams are mapped correctly and that knockout rows use slot references rather than guessed teams.</how-to-verify>
  <resume-signal>Type "approved" or list fixture discrepancies to correct.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q tests/test_ingest_elo.py tests/test_fixture.py`
- [ ] `python -m ruff check src/cdd_mundial/data tests`
- [ ] The Elo coverage report resolves all 48 participants
- [ ] The fixture has 104 unique matches and 72 group matches
- [ ] Human FIFA cross-check is approved
</verification>

<success_criteria>
- Elo TSV acquisition is bounded, cache-first, and reproducible.
- The current Elo snapshot resolves all tournament participants.
- The full fixture is canonical, UTC-normalized, structurally correct, and human-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-de-datos/01-03-SUMMARY.md`
</output>
