---
phase: 02-modelos-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/cdd_mundial/models/__init__.py
  - src/cdd_mundial/models/tournaments.py
  - src/cdd_mundial/models/loading.py
  - src/cdd_mundial/models/metrics.py
  - data/external/tournament_k_factors.csv
  - tests/test_tournaments.py
  - tests/test_loading.py
  - tests/test_metrics.py
autonomous: true
requirements: [MODEL-01, MODEL-04]
must_haves:
  truths:
    - "El venv importa scipy, sklearn, matplotlib, seaborn y joblib sin error"
    - "Los 200 strings de torneo del parquet real mapean a un K-factor o fallan ruidosamente"
    - "load_matches() entrega fechas datetime y etiqueta de outcome 90' (shootout/ET => empate)"
    - "RPS y Brier multiclase devuelven valores exactos contra casos calculados a mano"
  artifacts:
    - path: "src/cdd_mundial/models/tournaments.py"
      provides: "Clasificador torneo -> K-factor con fallo ruidoso"
      contains: "UnknownTournamentError"
    - path: "data/external/tournament_k_factors.csv"
      provides: "Tabla revisada de 200 strings de torneo -> categoria K"
    - path: "src/cdd_mundial/models/loading.py"
      provides: "load_matches() con outcome_90 y outcome_idx"
    - path: "src/cdd_mundial/models/metrics.py"
      provides: "rps() y brier_multiclass()"
  key_links:
    - from: "src/cdd_mundial/models/loading.py"
      to: "src/cdd_mundial/data/contracts.py"
      via: "HistoricalMatchesSchema.validate antes de mutar columnas"
      pattern: "HistoricalMatchesSchema\\.validate"
    - from: "src/cdd_mundial/models/tournaments.py"
      to: "data/external/tournament_k_factors.csv"
      via: "from_csv con default Path"
      pattern: "tournament_k_factors\\.csv"
---

<objective>
Fundaciones de la fase de modelos: instalar las dependencias faltantes (Wave 0), crear el paquete `cdd_mundial.models`, el clasificador torneo→K-factor (insumo del Elo, D-01), el cargador canónico del histórico con etiquetas de outcome de 90 minutos (D-05, pitfall 6), y las métricas custom RPS/Brier (MODEL-04).

Purpose: TODO lo demás en la fase depende de estos módulos. Separarlos en Wave 1 permite que Elo y Dixon-Coles corran en paralelo en Wave 2.
Output: paquete `models/` con tournaments/loading/metrics + tabla K revisada + 3 archivos de test verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modelos-baseline/02-CONTEXT.md
@src/cdd_mundial/data/contracts.py
@src/cdd_mundial/data/identities.py
</context>

<interfaces>
<!-- Contratos que este plan CREA y que los planes 02/03/04 consumen a ciegas. No cambiar firmas. -->

From src/cdd_mundial/models/tournaments.py (nuevo):
```python
class UnknownTournamentError(LookupError):
    """Raised when a tournament string has no reviewed K-factor assignment."""

class TournamentKTable:
    @classmethod
    def from_csv(cls, path: Path = Path("data/external/tournament_k_factors.csv")) -> "TournamentKTable": ...
    def k_factor(self, tournament: str) -> int:
        """Exact-match lookup; raises UnknownTournamentError otherwise."""
```

From src/cdd_mundial/models/loading.py (nuevo):
```python
OUTCOME_LABELS = ("home_win", "draw", "away_win")  # outcome_idx 0, 1, 2

def load_matches(path: Path = Path("data/processed/historical_matches.parquet")) -> pd.DataFrame:
    """Validated history + date (datetime64) + outcome_90 (str) + outcome_idx (int)."""
```

From src/cdd_mundial/models/metrics.py (nuevo):
```python
def rps(probs: np.ndarray, outcome_idx: np.ndarray) -> float: ...
def brier_multiclass(probs: np.ndarray, outcome_idx: np.ndarray) -> float: ...
# probs: (n, 3) en orden [p_home_win, p_draw, p_away_win]; outcome_idx: (n,) en {0,1,2}
```

From src/cdd_mundial/data/contracts.py (existente — columnas reales del parquet):
match_id, date (str), home_team_id, away_team_id, home_team_source_name, away_team_source_name,
home_score (int>=0), away_score (int>=0), tournament, city, country, neutral (bool),
shootout_winner_team_id (str|null), result_after_extra_time (bool), source, source_version
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dependencias Wave 0 en pyproject + instalación editable</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (estado actual de [project] dependencies y [project.optional-dependencies])
  </read_first>
  <action>
    Añadir a `[project] dependencies` de pyproject.toml, con el mismo estilo de pin existente, exactamente estas 5 entradas (RESEARCH §Environment Availability — scipy/sklearn/matplotlib/seaborn/joblib están AUSENTES del venv, bloqueante):
    - "scipy~=1.17"
    - "scikit-learn~=1.9"
    - "matplotlib~=3.10"
    - "seaborn==0.13.2"
    - "joblib>=1.4"
    NO añadir penaltyblog ni statsmodels a pyproject (solo instalación manual opcional, nunca pineados — constraint del stack).
    NO tocar los pins existentes (pandas~=2.3.3, numpy>=2,<2.5).
    Luego instalar: `.\.venv\python.exe -m pip install -e ".[dev]"`.
  </action>
  <verify>
    <automated>.\.venv\python.exe -c "import scipy, sklearn, matplotlib, seaborn, joblib; print('wave0-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contiene la línea `"scipy~=1.17"` y la línea `"seaborn==0.13.2"`
    - pyproject.toml NO contiene `penaltyblog` ni `statsmodels`
    - El comando de verify imprime `wave0-ok` con exit code 0
    - `.\.venv\python.exe -m pytest -q` sigue verde (la suite Phase 1 no se rompe por la instalación)
  </acceptance_criteria>
  <done>Las 5 dependencias instaladas en el venv y la suite existente verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clasificador torneo→K-factor + tabla revisada de 200 strings</name>
  <files>src/cdd_mundial/models/__init__.py, src/cdd_mundial/models/tournaments.py, data/external/tournament_k_factors.csv, tests/test_tournaments.py</files>
  <read_first>
    - src/cdd_mundial/data/identities.py (patrón completo: excepciones nombradas líneas 14-19, from_csv con default Path líneas 39-65, resolve con fallo ruidoso líneas 92-101)
    - data/external/teams.csv (primeras 3 líneas — convención CSV: header snake_case, valores revisados)
    - tests/test_identities.py (forma de tests: pytest.raises con match=)
  </read_first>
  <behavior>
    - Test 1: `TournamentKTable.from_csv().k_factor("FIFA World Cup") == 60`
    - Test 2: `k_factor("Friendly") == 20` (verificar el string exacto del parquet; si es "Friendly" usar ese)
    - Test 3: `k_factor("FIFA World Cup qualification") == 40`
    - Test 4: `k_factor("Copa América") == 50` (string exacto CON tilde UTF-8 — pitfall 5)
    - Test 5: string desconocido lanza `UnknownTournamentError` con match="unknown tournament"
    - Test 6 (marker data_acceptance): TODOS los strings únicos de `tournament` en data/processed/historical_matches.parquet resuelven sin excepción (cobertura de los ~200 strings)
  </behavior>
  <action>
    1. Crear `src/cdd_mundial/models/__init__.py` con SOLO el docstring de una línea:
       `"""Baseline structural models: dynamic Elo, Dixon-Coles, metrics, validation."""`
       (el re-export de predict_lambdas lo añade el plan 02-03 — no anticiparlo aquí).
    2. Crear `src/cdd_mundial/models/tournaments.py` siguiendo el patrón de identities.py:
       - Docstring de módulo de una línea; `from __future__ import annotations`; type hints completos.
       - Constante `K_CATEGORIES = {"wc": 60, "continental": 50, "qualifier_major": 40, "other": 30, "friendly": 20}`.
         Esta es la tabla CANÓNICA WFE de 5 niveles (60/50/40/30/20) — D-01 ordena "seguir la fuente canónica WFE"
         y la canónica difiere de la tabla literal de D-01 (continentales = 50, no 60; pitfall 3 de RESEARCH).
       - `class UnknownTournamentError(LookupError)` con docstring de una línea.
       - `class TournamentKTable` con `from_csv(path=Path("data/external/tournament_k_factors.csv"))`,
         que valida que toda fila tenga `k_category` en K_CATEGORIES y `k_factor == K_CATEGORIES[k_category]`
         (ValueError si no), y `k_factor(tournament: str) -> int` por igualdad EXACTA de string
         (con tildes UTF-8 intactas) que lanza `UnknownTournamentError(f"unknown tournament: {tournament!r}")`.
    3. Generar `data/external/tournament_k_factors.csv` (header: `tournament,k_category,k_factor,note`) con un
       snippet one-off (puede correr inline, no se versiona como módulo): leer
       `pd.read_parquet("data/processed/historical_matches.parquet")["tournament"].unique()` (~200 strings) y asignar:
       - string exacto "FIFA World Cup" → wc,60
       - finales continentales (asignar tras inspeccionar los strings reales): "Copa América", "UEFA Euro",
         "African Cup of Nations", "AFC Asian Cup", "Gold Cup", "Confederations Cup", "Oceania Nations Cup"
         y variantes exactas presentes → continental,50
       - strings que contienen "qualification" (cualquier confederación) → qualifier_major,40
       - "UEFA Nations League" y "CONCACAF Nations League" → qualifier_major,40 (assumption A2 del RESEARCH:
         Nations League = torneo mayor K=40; anotar "A2" en la columna note)
       - string exacto "Friendly" → friendly,20
       - TODO lo demás (cola larga: CECAFA Cup, Gulf Cup, King's Cup, Merdeka Tournament, etc.) → other,30
       Escribir el CSV con encoding UTF-8 (tildes intactas), ordenado alfabéticamente por tournament, y commitearlo
       como tabla revisada (patrón teams.csv).
    4. Crear `tests/test_tournaments.py` con los 6 tests del behavior. El test 6 lleva
       `@pytest.mark.data_acceptance` (requiere el parquet real) e itera todos los strings únicos llamando k_factor.
  </action>
  <verify>
    <automated>.\.venv\python.exe -m pytest tests/test_tournaments.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UnknownTournamentError" src/cdd_mundial/models/tournaments.py` >= 2 (definición + uso)
    - tournaments.py contiene `"wc": 60` y `"continental": 50` y `"friendly": 20`
    - data/external/tournament_k_factors.csv existe, tiene header `tournament,k_category,k_factor,note` y >= 150 filas de datos
    - El CSV contiene la fila de `Copa América` (con tilde) con k_factor 50
    - `pytest tests/test_tournaments.py -x -q` exit 0 (incluye el data_acceptance de cobertura total)
  </acceptance_criteria>
  <done>Los ~200 strings de torneo del parquet real mapean a K ∈ {20,30,40,50,60}; desconocidos fallan ruidosamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: load_matches() con outcome 90' + métricas RPS/Brier custom</name>
  <files>src/cdd_mundial/models/loading.py, src/cdd_mundial/models/metrics.py, tests/test_loading.py, tests/test_metrics.py</files>
  <read_first>
    - src/cdd_mundial/data/contracts.py (HistoricalMatchesSchema — columnas y tipos exactos; `date` es str)
    - tests/conftest.py (fixture test_workspace existente — reutilizar, no redefinir)
    - tests/test_identities.py (patrón de construcción de DataFrames sintéticos inline)
  </read_first>
  <behavior>
    - Test 1 (loading): partido 2-1 sin ET → outcome_90 == "home_win", outcome_idx == 0
    - Test 2 (loading): partido 3-3 con shootout_winner_team_id no nulo → outcome_90 == "draw", outcome_idx == 1
    - Test 3 (loading): partido 2-1 con result_after_extra_time=True → outcome_90 == "draw" (el marcador es FT+ET; el 90' fue empate — pitfall 6)
    - Test 4 (loading): la columna date sale como datetime64, no str
    - Test 5 (metrics): pronóstico perfecto probs=[1,0,0], outcome 0 → rps == 0.0 y brier == 0.0
    - Test 6 (metrics): uniforme [1/3,1/3,1/3] con outcome 0 → rps == pytest.approx(5/18) y brier == pytest.approx(2/3)
    - Test 7 (metrics): rps penaliza más el error "lejano": probs=[0.8,0.2,0] outcome=2 da rps mayor que probs=[0,0.2,0.8] outcome=2
  </behavior>
  <action>
    1. Crear `src/cdd_mundial/models/loading.py`:
       - `OUTCOME_LABELS = ("home_win", "draw", "away_win")` (índices 0/1/2).
       - `def load_matches(path: Path = Path("data/processed/historical_matches.parquet")) -> pd.DataFrame:`
         (a) `frame = HistoricalMatchesSchema.validate(pd.read_parquet(path))` — validar ANTES de mutar
             (el schema es strict=True; añadir columnas después de validar, pitfall 11);
         (b) `frame["date"] = pd.to_datetime(frame["date"], errors="raise")` — conversión ÚNICA;
         (c) `drew_90 = frame["result_after_extra_time"] | frame["shootout_winner_team_id"].notna()`
             — regla D-05/pitfall 6: si hubo ET o penales, el resultado de 90' fue empate; los goles registrados
             son FT+ET y NO definen el outcome en esos 677 partidos;
         (d) `outcome_90` = "draw" si drew_90; si no, "home_win"/"draw"/"away_win" por comparación
             home_score vs away_score; `outcome_idx` = índice en OUTCOME_LABELS (int).
         (e) Devolver el frame ordenado por (date, match_id) ascendente (determinismo para el Elo secuencial).
       - También aceptar `frame: pd.DataFrame` precargado vía parámetro opcional para tests sintéticos
         (firma: `load_matches(path=..., frame: pd.DataFrame | None = None)`; si frame no es None, validar y procesar ese).
    2. Crear `src/cdd_mundial/models/metrics.py` copiando VERBATIM las funciones verificadas de RESEARCH §Code Examples:
       ```python
       def rps(probs: np.ndarray, outcome_idx: np.ndarray) -> float:
           """probs: (n, 3) en orden [home_win, draw, away_win]; outcome_idx: (n,) en {0,1,2}."""
           cum_p = np.cumsum(probs, axis=1)
           cum_o = np.cumsum(np.eye(3)[outcome_idx], axis=1)
           return float(((cum_p - cum_o) ** 2).sum(axis=1).mean() / 2)

       def brier_multiclass(probs: np.ndarray, outcome_idx: np.ndarray) -> float:
           return float(((probs - np.eye(3)[outcome_idx]) ** 2).sum(axis=1).mean())
       ```
       log-loss NO se implementa aquí — se importa de sklearn.metrics donde se necesite (Don't Hand-Roll).
    3. Crear `tests/test_loading.py` (tests 1-4, DataFrames sintéticos inline que pasen HistoricalMatchesSchema —
       incluir TODAS las 16 columnas requeridas por strict=True) y `tests/test_metrics.py` (tests 5-7 con los
       valores exactos a mano: uniforme outcome 0 ⇒ rps = ((1/3-1)^2+(2/3-1)^2)/2 = 5/18 ≈ 0.2778, brier = 6/9 = 2/3).
  </action>
  <verify>
    <automated>.\.venv\python.exe -m pytest tests/test_loading.py tests/test_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - loading.py contiene `HistoricalMatchesSchema.validate` y `pd.to_datetime`
    - loading.py contiene `result_after_extra_time` y `shootout_winner_team_id` en la regla de empate
    - metrics.py contiene `def rps(` y `def brier_multiclass(` y NO contiene `def log_loss`
    - `pytest tests/test_loading.py tests/test_metrics.py -x -q` exit 0 con >= 7 tests pasando
  </acceptance_criteria>
  <done>Cargador canónico y métricas listos para que Elo, DC y validación los consuman sin reimplementar nada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parquet → modelos | Datos históricos entran al pipeline de modelado; un esquema corrupto envenena todo downstream |
| PyPI → venv | Dependencias nuevas entran a la cadena de suministro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | loading.load_matches | mitigate | `HistoricalMatchesSchema.validate` (strict+coerce) en cada carga; deriva de esquema falla ruidosamente |
| T-02-02 | Spoofing | tournaments.k_factor | mitigate | Igualdad exacta de string + UnknownTournamentError; ningún matching difuso ni default silencioso |
| T-02-03 | Tampering | pyproject deps (supply chain) | mitigate | Solo las 5 deps de la lista Wave 0, con pins; penaltyblog/statsmodels excluidos de pyproject (ASVS V14) |
| T-02-04 | Tampering | data/raw inmutable | accept | Esta fase solo LEE data/processed; ninguna escritura toca raw (invariante Phase 1 vigente) |
</threat_model>

<verification>
- `.\.venv\python.exe -m pytest tests/test_tournaments.py tests/test_loading.py tests/test_metrics.py -x -q` — verde
- `.\.venv\python.exe -m pytest -q` — suite completa verde (Phase 1 no regresiona)
- `.\.venv\python.exe -c "from cdd_mundial.models.tournaments import TournamentKTable; print(TournamentKTable.from_csv().k_factor('FIFA World Cup'))"` imprime 60
</verification>

<success_criteria>
- 5 dependencias Wave 0 instaladas y pineadas en pyproject
- Cobertura total: 100% de los strings de torneo del parquet real mapean a K
- load_matches etiqueta correctamente los 677 partidos shootout/ET como empate de 90'
- RPS/Brier exactos contra valores a mano
</success_criteria>

<output>
After completion, create `.planning/phases/02-modelos-baseline/02-01-SUMMARY.md`
</output>
