---
phase: 02-modelos-baseline
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - notebooks/02_modelos_baseline.ipynb
  - tests/test_notebooks.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04]
must_haves:
  truths:
    - "El notebook 02 ejecuta limpio end-to-end con jupyter nbconvert --execute"
    - "El notebook deriva la correccion rho, la log-likelihood ponderada y la estructura del gradiente en celdas markdown con LaTeX (D-14)"
    - "El notebook incluye reliability diagram sobre los holdouts y el check de sanidad de empates 24-28%"
    - "Toda celda de codigo esta envuelta en 'What and why' / 'Interpretation' y solo INVOCA funciones de cdd_mundial.models"
    - "El gate estructural de fragmentos prohibidos aplica tambien al notebook 02"
  artifacts:
    - path: "notebooks/02_modelos_baseline.ipynb"
      provides: "Material didactico D-14: derivaciones + visuales + interpretacion del gate"
    - path: "tests/test_notebooks.py"
      provides: "Gate de fragmentos prohibidos extendido al notebook 02"
      contains: "02_modelos_baseline"
  key_links:
    - from: "notebooks/02_modelos_baseline.ipynb"
      to: "src/cdd_mundial/models/"
      via: "imports de cdd_mundial.models (nunca redefine logica)"
      pattern: "from cdd_mundial.models"
    - from: "notebooks/02_modelos_baseline.ipynb"
      to: "data/processed/models/holdout_predictions_*.parquet"
      via: "reliability diagram sin refit"
      pattern: "holdout_predictions"
---

<objective>
Notebook didáctico de la fase (D-14/D-15): derivar la matemática core (corrección ρ de Dixon-Coles, log-likelihood ponderada, gradiente), visualizar resultados (reliability diagram, heatmap de marcadores, comparativa del gate) e interpretar las decisiones (ξ y su half-life corregido, margen 538 con atribución, empates 24-28%). Extender el gate estructural de fragmentos prohibidos al notebook 02.

Purpose: el contenido pedagógico es el diferenciador de portafolio del proyecto — éste es el entregable de aprendizaje de la fase.
Output: notebook ejecutable que pasa todos los gates + gate extendido en tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modelos-baseline/02-CONTEXT.md
@notebooks/01_data_foundation.ipynb
@tests/test_notebooks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notebook 02 — derivaciones, visuales y checks de sanidad</name>
  <files>notebooks/02_modelos_baseline.ipynb</files>
  <read_first>
    - notebooks/01_data_foundation.ipynb (estructura de celdas EXACTA a replicar: headers MD con "What and why:", celda de bootstrap ROOT/os.chdir, "Interpretation:" después de cada código; kernelspec python3)
    - tests/test_notebooks.py (gates automáticos que el notebook DEBE pasar: strings literales "What and why" e "Interpretation", sin celdas vacías, sin secretos, kernel python3)
    - data/processed/models/validation_report_2026-06-12.json (resultados reales del gate a interpretar — usar glob validation_report_*.json)
  </read_first>
  <action>
    Crear `notebooks/02_modelos_baseline.ipynb` (kernelspec python3, texto en español, marcadores estructurales
    "What and why:" / "Interpretation:" EN INGLÉS EXACTO — el gate los busca como strings literales).

    Restricción dura (D-15): NINGUNA celda de código contiene `def `, `class `, `import requests`,
    `import kagglehub` ni `os.environ`. El código solo importa e INVOCA `cdd_mundial.models.*` y grafica con
    matplotlib/seaborn (NUNCA plotly). Las derivaciones van en markdown con LaTeX.

    Secciones (cada celda de código envuelta en su par MD):

    1. **Intro MD**: objetivo de la fase, los 4 requirements, el contrato predict_lambdas como entregable.
    2. **Bootstrap**: celda de código copiada del notebook 01 (ROOT = Path.cwd()... + os.chdir(ROOT))
       con su par What and why / Interpretation, igual que en el 01.
    3. **Elo dinámico (MODEL-01)**:
       - MD: fórmula We = 1/(10^(-dr/400)+1), tabla K canónica WFE (60/50/40/30/20) y por qué difiere de la
         tabla literal de D-01 (cláusula "seguir la fuente canónica"), ventaja local +100 condicional a
         `neutral` (reconciliación del Director: histórico WFE estándar; en 2026 solo MEX/USA/CAN),
         multiplicador de margen = variante FiveThirtyEight CON ATRIBUCIÓN (no es la tabla G de eloratings.net)
         y la rama de empate factor 1.0 (pitfall: log(1)=0 congelaría el 22.7% de los partidos),
         shootout/ET cuenta como empate W=0.5 (D-05).
       - Código: cargar elo_ratings.parquet recomputado y elo_current.parquet; scatter de rangos + Spearman
         sobre las 48 selecciones; top-10 barras.
       - Interpretación: por qué se comparan RANGOS y no niveles (cold-start 1000 vs media ~1500 del snapshot).
    4. **Dixon-Coles — derivación (D-14, el corazón pedagógico)**:
       - MD con LaTeX: modelo P(X=x, Y=y) = τ_{λ,μ,ρ}(x,y) · Poisson(x;λ) · Poisson(y;μ);
         las 4 celdas de τ (ecs. 4.2-4.3 del paper, citado: Dixon & Coles 1997); por qué la independencia
         falla en marcadores bajos; log λ = c + att_i − def_j + γ·1[local]; la log-likelihood ponderada
         ℓ = Σ w_m [log τ + x log λ − λ + y log μ − μ] con w_m = exp(−ξ·Δt); la estructura del gradiente
         (∂ℓ/∂att_i acumula (x−λ) sobre los partidos del equipo i; τ solo aporta en marcadores ≤ 1);
         identifiabilidad (Σatt = Σdef = 0) y las cotas de ρ.
       - MD aparte: **ξ y half-life con la aritmética CORREGIDA** — half-life = ln(2)/ξ; ξ=0.0018/d ⇒ 385 días
         (~1.05 años, el óptimo del paper DC), ξ=0.00095/d ⇒ ~2 años; el D-06 original mezclaba ambos números
         (pitfall 4) y el mini-grid sobre holdouts lo resolvió con evidencia — leer chosen_xi del
         validation_report y mostrar la comparativa de log-loss por ξ.
       - Código: load_production_model(); predict_lambdas para 2-3 cruces ilustrativos (p.ej. argentina-mexico
         neutral, mexico-X con neutral=False); score_matrix como heatmap seaborn de un cruce.
       - Interpretación: lectura de λs y del efecto γ del anfitrión.
    5. **W/D/L y sanidad de empates (MODEL-03)**:
       - Código: wdl_from_lambdas sobre pares de selecciones de Elo similar en cancha neutral; mostrar que
         P(empate) cae en [0.24, 0.28] (check del CONTEXT §specifics).
       - Interpretación: el empate sale del modelo de goles, NO de un logistic sobre Elo (D-10).
    6. **Validación temporal y gate D-13 (MODEL-04)**:
       - Código: cargar validation_report_*.json y holdout_predictions_*.parquet (glob, tomar el más reciente);
         tabla log-loss/Brier/RPS por holdout × modelo (DataFrame estilizado); barras comparativas.
       - Código: reliability diagram — con sklearn.calibration.calibration_curve one-vs-rest por clase
         (home_win/draw/away_win) sobre las predicciones DC agrupadas de los 4 holdouts (n_bins=10), 3 curvas +
         diagonal en un solo axes matplotlib.
       - Interpretación: resultado HONESTO del gate (pase o falle — si falla, qué significa y por qué se
         continúa, D-13); qué dice el reliability diagram de la honestidad de las probabilidades; nota de que
         el baseline solo-ranking-FIFA quedó excluido por decisión del Director (sin datos point-in-time
         ingestados) como stretch post-fase.
    7. **Cierre MD**: qué queda congelado para Phase 3 (firma D-09), qué consume Phase 5 (elo_history, λs).

    Ejecutar y guardar con outputs:
    `.\.venv\python.exe -m jupyter nbconvert --to notebook --execute --inplace notebooks/02_modelos_baseline.ipynb`
  </action>
  <verify>
    <automated>.\.venv\python.exe -m jupyter nbconvert --to notebook --execute --stdout notebooks/02_modelos_baseline.ipynb >$null; .\.venv\python.exe -m pytest tests/test_notebooks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - notebooks/02_modelos_baseline.ipynb existe y nbconvert --execute sale con exit 0
    - El notebook contiene los strings "What and why" e "Interpretation" (gate estructural verde)
    - El notebook contiene "ln(2)" (aritmética de half-life corregida) y "FiveThirtyEight" (atribución del margen)
    - El notebook contiene "calibration_curve" y "wdl_from_lambdas"
    - Ninguna celda de código del notebook contiene "def " ni "class "
    - `pytest tests/test_notebooks.py -x -q` exit 0
  </acceptance_criteria>
  <done>Notebook didáctico completo: derivaciones D-14, visuales de calibración y lectura honesta del gate.</done>
</task>

<task type="auto">
  <name>Task 2: Extender el gate de fragmentos prohibidos al notebook 02 + cierre de fase</name>
  <files>tests/test_notebooks.py</files>
  <read_first>
    - tests/test_notebooks.py (líneas 47-53: FORBIDDEN_CODE_FRAGMENTS; líneas 120-129: aplicación hoy limitada al notebook 01 — entender el mecanismo exacto antes de tocar)
  </read_first>
  <action>
    1. Modificar tests/test_notebooks.py para que el gate de fragmentos prohibidos (`def `, `class `,
       `import requests`, `import kagglehub`, `os.environ`, etc.) aplique TAMBIÉN a
       `notebooks/02_modelos_baseline.ipynb` — recomendación explícita de RESEARCH Pattern 3 y PATTERNS.md.
       Mecanismo según lo que exista en el archivo: si la aplicación está parametrizada por nombre de notebook,
       añadir el 02 a la lista/parametrización; mantener el resto del archivo intacto.
    2. Correr la suite COMPLETA de la fase como gate de cierre:
       `.\.venv\python.exe -m pytest -q` — todo verde (unit + data_acceptance + notebooks).
  </action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notebooks.py contiene la cadena "02_modelos_baseline" en la aplicación del gate de fragmentos
    - `pytest -q` exit 0 — suite completa del repo verde
  </acceptance_criteria>
  <done>El notebook 02 queda bajo los mismos gates estructurales que el 01; la fase cierra con suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook → repo público GitHub | Los outputs renderizados se publican; superficie de fuga de información |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Information Disclosure | outputs del notebook publicados | mitigate | Gate existente test_notebook_contains_no_secret_material escanea también outputs — corre automático sobre el 02 |
| T-02-19 | Tampering | notebook redefine lógica divergente de src/ | mitigate | Gate de fragmentos prohibidos extendido al 02 (Task 2): el notebook solo invoca, nunca define |
</threat_model>

<verification>
- `.\.venv\python.exe -m jupyter nbconvert --to notebook --execute --stdout notebooks/02_modelos_baseline.ipynb` exit 0
- `.\.venv\python.exe -m pytest -q` — suite completa verde
</verification>

<success_criteria>
- D-14 cumplido: ρ, log-likelihood y gradiente derivados en el notebook a nivel ingeniería/maestría
- D-15 cumplido: estructura MD→código→MD verificada por gates automáticos; cero lógica definida en el notebook
- Reliability diagram y check de empates 24-28% presentes e interpretados
- Suite completa del repo verde — fase lista para /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/02-modelos-baseline/02-05-SUMMARY.md`
</output>
