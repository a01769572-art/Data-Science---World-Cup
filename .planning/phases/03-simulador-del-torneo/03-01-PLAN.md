---
phase: 03-simulador-del-torneo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/metadata/fifa_2026_regulations.provenance.json
  - tests/fixtures/tournament/third_place_expected_combinations.json
  - tests/fixtures/tournament/third_place_mapping_official.json
  - tests/validators/validate_third_place_mapping.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "The official FIFA 2026 rule evidence is pinned locally with an official FIFA HTTPS URL, retrieval timestamp, checksum, and explicit article/annex pointers before any simulator rule is trusted (D-03, D-04)."
    - "An independently authored expected-combinations fixture derived from the official annex enumerates the exact supported eight-of-twelve qualifying-group sets before any slot mapping is trusted (D-06)."
    - "The best-third assignment artifact is reviewed, uses explicit real Round-of-32 selector positions from the frozen fixture, is exhaustive for the independently authored expected combinations, bijective per case, and compatible with the official slot tokens (D-06)."
  artifacts:
    - path: data/metadata/fifa_2026_regulations.provenance.json
      provides: "Immutable official-rule provenance with URL, retrieval timestamp, SHA-256, article pointers, annex pointers, and reviewed extraction fields"
    - path: tests/fixtures/tournament/third_place_expected_combinations.json
      provides: "Independently authored expected eight-of-twelve qualifying-group combinations extracted from the official annex"
    - path: tests/fixtures/tournament/third_place_mapping_official.json
      provides: "Reviewed official best-third assignment lookup with provenance, explicit selector-position assignments, exhaustive combination coverage, bijective assignments, and token-compatibility evidence"
    - path: tests/validators/validate_third_place_mapping.py
      provides: "PowerShell-safe contract validator that cross-checks provenance, expected combinations, real fixture selectors, mapping coverage, bijection, and token compatibility"
  key_links:
    - from: tests/fixtures/tournament/third_place_expected_combinations.json
      to: tests/fixtures/tournament/third_place_mapping_official.json
      via: "independent expected combinations consumed by the validator instead of inferred from the mapping file itself"
      pattern: "qualified_groups"
    - from: tests/fixtures/tournament/third_place_mapping_official.json
      to: data/external/fixture_2026.csv
      via: "explicit reviewed selector positions matched against the real Round-of-32 fixture rows"
      pattern: "selector_key"
    - from: data/metadata/fifa_2026_regulations.provenance.json
      to: tests/fixtures/tournament/third_place_expected_combinations.json
      via: "annex references that justify the independently authored expected combinations"
      pattern: "annex_refs"
    - from: data/metadata/fifa_2026_regulations.provenance.json
      to: tests/fixtures/tournament/third_place_mapping_official.json
      via: "annex references and review metadata that justify each explicit selector assignment"
      pattern: "reviewed_extraction"
---

<objective>
Capture the unresolved FIFA regulation evidence as a standalone fail-closed gate before any rules or engine task is allowed to trust the 2026 tie-break or best-third mapping.

Purpose: Remove the blocker explicitly called out in `STATE.md` and prevent guessed bracket logic from entering the codebase.
Output: Official-rule provenance artifact, independent expected-combinations fixture, reviewed best-third mapping artifact, and a validator script that fails closed.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simulador-del-torneo/03-CONTEXT.md
@.planning/phases/03-simulador-del-torneo/03-RESEARCH.md
@.planning/phases/03-simulador-del-torneo/03-VALIDATION.md
@.planning/phases/03-simulador-del-torneo/03-PATTERNS.md

<interfaces>
From `src/cdd_mundial/data/provenance.py`:

```python
@dataclass(frozen=True)
class ProvenanceRecord:
    ...
    def to_dict(self) -> dict[str, object]:
        ...
```

From `src/cdd_mundial/data/ingest_fixture.py`:

```python
FIXTURE_COLUMNS = [...]
def load_fixture_2026(fixture_path: Path = Path("data/external/fixture_2026.csv")) -> pd.DataFrame:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin official-rule provenance in a committed fail-closed manifest</name>
  <files>data/metadata/fifa_2026_regulations.provenance.json, tests/validators/validate_third_place_mapping.py</files>
  <action>Following the deterministic JSON provenance pattern from `src/cdd_mundial/data/provenance.py` and the assertion style in `tests/test_provenance.py`, capture the official FIFA rule evidence as a committed artifact. Per D-03 and D-04, prefer a small extracted JSON over committing the raw PDF when redistribution is unclear, but fail closed unless the artifact includes an official FIFA HTTPS `source_url`, retrieval timestamp, non-empty SHA-256, explicit article pointers for the group tie-break and fair-play rules, explicit annex pointers for the best-third assignment table, and reviewed retrieval/review metadata that records who extracted the annex evidence and when. In the same task, create the first version of `tests/validators/validate_third_place_mapping.py` with a `provenance` mode so the plan verifies the manifest through a committed validator instead of brittle inline shell quoting. Do not treat secondary sources or token inference as authority.</action>
  <verify>
    <automated>.\.venv\python.exe tests/validators/validate_third_place_mapping.py provenance</automated>
  </verify>
  <done>The regulatory blocker in `STATE.md` is converted into a concrete immutable artifact that fails closed on missing provenance fields.</done>
</task>

<task type="auto">
  <name>Task 2: Encode independent expected combinations plus the reviewed official best-third mapping</name>
  <files>tests/fixtures/tournament/third_place_expected_combinations.json, tests/fixtures/tournament/third_place_mapping_official.json, tests/validators/validate_third_place_mapping.py</files>
  <action>Following the reviewable fixture pattern in `tests/fixtures/fixture/fixture_2026_sample.csv`, the provenance payload pattern from `src/cdd_mundial/data/provenance.py`, and the frozen slot-contract rules in `src/cdd_mundial/data/ingest_fixture.py`, commit two separate reviewable artifacts plus a validator. First, author `third_place_expected_combinations.json` as an independently reviewed extraction from the official annex that enumerates the exact supported eight-of-twelve qualifying-group sets, with no selector assignments and no derivation from the mapping file. Second, author `third_place_mapping_official.json` as the reviewed lookup that records provenance back to the official rule manifest and contains explicit Round-of-32 selector assignments keyed by the real fixture selector positions from `fixture_2026.csv` (or stable `match_id` + side keys if selector token strings are not unique), not inferred perfect matchings. Each mapping case must assign every qualifying third-place group exactly once, cover every independently authored qualifying-group set, and include token-compatibility annotations tied to the real `3...` slot token on the targeted fixture row. Third, add `tests/validators/validate_third_place_mapping.py` as the executor-safe contract checker that reads the provenance manifest, the independent expected-combinations fixture, the mapping fixture, and `data/external/fixture_2026.csv`, then fails closed on unknown selectors, missing supported combinations, extra combinations, duplicate selector use inside a case, duplicate group assignment inside a case, or token-incompatible assignments. Do not guess concrete official data in the plan; the executor must capture and review it from the official annex before encoding.</action>
  <verify>
    <automated>.\.venv\python.exe tests/validators/validate_third_place_mapping.py mapping</automated>
  </verify>
  <done>The independent expected-combinations fixture and reviewed mapping artifact are separately reviewable, explicit about real selector positions, exhaustive for the supported combinations, bijective per case, and token-compatible against the frozen fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external FIFA document -> local provenance fixture | Untrusted remote content becomes authoritative local evidence |
| reviewed mapping artifact -> future slot resolver | A manually reviewed table constrains later bracket resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `third_place_mapping_official.json` | mitigate | Require official-source provenance fields, independent expected-combination coverage, explicit selector-position assignments, bijection, and token-compatibility evidence before mapping is trusted. |
| T-03-02 | Repudiation | regulation capture | mitigate | Store retrieval timestamp, official URL, SHA-256, article pointers, and annex pointers in deterministic JSON. |
| T-03-03 | Tampering | provenance evidence | mitigate | Accept only official FIFA HTTPS sources and reviewed extraction fields; fail closed otherwise. |
| T-03-04 | Tampering | independent expected combinations | mitigate | Keep expected supported group sets in a separate artifact so mapping coverage is checked against an independently authored annex extraction. |
</threat_model>

<verification>
Run the fail-closed regulatory checks after writing the artifacts:

```powershell
.\.venv\python.exe tests/validators/validate_third_place_mapping.py all
```
</verification>

<success_criteria>
- The official regulatory blocker is converted into pinned local evidence.
- The committed expected-combinations fixture is independent from the mapping artifact and traceable to the official annex.
- The committed mapping artifact is exhaustive, explicit about real selector positions, bijective, and token-compatible.
- No task guesses the annex from fixture tokens alone.
</success_criteria>

<source_audit>
GOAL
- Covered here: official-rule verification prerequisite for the 2026 format.

REQ
- Covered here: `SIM-01`.
- Covered in later plans: the executable rules tests and implementation, plus `SIM-02`, `SIM-03`, `SIM-04`, `SIM-05`.

RESEARCH
- Covered here: official annex/PDF gate, extracted-artifact preference, token-family ambiguity.

CONTEXT
- Covered here: `D-03`, `D-04`, `D-06`.
- Covered in later plans: `D-01`, `D-02`, `D-05`, `D-07`, `D-08`, `D-09`, `D-10`, `D-11`.

DEFERRED IDEAS
- Explicit extra-time scoring model and joint group-state tables remain excluded.
</source_audit>

<output>
After completion, create `.planning/phases/03-simulador-del-torneo/03-01-SUMMARY.md`
</output>
