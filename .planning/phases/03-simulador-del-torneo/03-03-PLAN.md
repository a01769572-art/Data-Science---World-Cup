---
phase: 03-simulador-del-torneo
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/fixtures/tournament/wc2018_group_h.json
  - tests/fixtures/tournament/two_way_head_to_head.json
  - tests/fixtures/tournament/three_way_tie.json
  - tests/fixtures/tournament/four_way_tie.json
  - tests/fixtures/tournament/fair_play_tie.json
  - tests/fixtures/tournament/fifa_ranking_tie.json
  - tests/fixtures/tournament/best_thirds.json
  - tests/test_rules_fifa.py
  - tests/test_slot_resolution.py
  - tests/validators/assert_phase03_red.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Wave 0 rules and slot tests exist before the pure rules implementation begins, so Phase 3 follows the Nyquist requirement from `03-VALIDATION.md`."
    - "Historical and synthetic fixtures encode exact expected rankings, conduct-score behavior, FIFA-ranking fallback, and reviewed best-third ordering without reconstructing expectations from production logic (D-03, D-04)."
  artifacts:
    - path: tests/test_rules_fifa.py
      provides: "Failing rules tests for standings, conduct score, FIFA-ranking fallback, and best-third ranking"
    - path: tests/test_slot_resolution.py
      provides: "Failing slot-resolution tests for fixture tokens and official third-place assignment"
    - path: tests/validators/assert_phase03_red.py
      provides: "PowerShell-safe red-test validator that executes selected nodes and accepts only expected missing `rules_fifa` / `slots` failures"
  key_links:
    - from: tests/fixtures/tournament/best_thirds.json
      to: tests/test_rules_fifa.py
      via: "expected ordered qualifiers and criterion annotations"
      pattern: "best_thirds"
    - from: tests/fixtures/tournament/third_place_mapping_official.json
      to: tests/test_slot_resolution.py
      via: "provenance-backed official assignment cases and exhaustive compatibility checks"
      pattern: "third_place_mapping_official"
---

<objective>
Create the Wave 0 fixtures and failing tests for SIM-01 once the official evidence and package bootstrap exist.

Purpose: Make the pure rules implementation deterministic and reviewable before code is written.
Output: Historical/synthetic JSON fixtures, failing rules/slot tests, and a red-test validator script that proves the authored nodes fail for the intended missing behaviors.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-simulador-del-torneo/03-CONTEXT.md
@.planning/phases/03-simulador-del-torneo/03-RESEARCH.md
@.planning/phases/03-simulador-del-torneo/03-VALIDATION.md
@.planning/phases/03-simulador-del-torneo/03-PATTERNS.md
@.planning/phases/03-simulador-del-torneo/03-01-PLAN.md
@.planning/phases/03-simulador-del-torneo/03-02-PLAN.md

<interfaces>
From `src/cdd_mundial/simulation/__init__.py` created in `03-02`:

```python
__all__ = [...]
```

From `src/cdd_mundial/data/ingest_fixture.py`:

```python
def validate_fixture_structure(fixture: pd.DataFrame) -> pd.DataFrame:
def load_fixture_2026(fixture_path: Path = Path("data/external/fixture_2026.csv")) -> pd.DataFrame:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reviewable group and best-third fixtures plus failing rules tests</name>
  <files>tests/fixtures/tournament/wc2018_group_h.json, tests/fixtures/tournament/two_way_head_to_head.json, tests/fixtures/tournament/three_way_tie.json, tests/fixtures/tournament/four_way_tie.json, tests/fixtures/tournament/fair_play_tie.json, tests/fixtures/tournament/fifa_ranking_tie.json, tests/fixtures/tournament/best_thirds.json, tests/test_rules_fifa.py, tests/validators/assert_phase03_red.py</files>
  <behavior>
    - Test 1: historical and synthetic fixtures resolve the exact expected group order through the official Art. 13 sequence, conduct score, and successive FIFA-ranking editions.
    - Test 2: best-third ranking selects exactly eight teams in the expected order and never applies head-to-head across groups.
    - Test 3: expected rankings are read from fixture payloads, not reconstructed with production logic.
  </behavior>
  <action>Use the minimal fixture-factory pattern from `tests/test_contracts.py`, the fail-loud assertion style from `tests/test_fixture.py`, and the JSON fixture conventions mapped in `03-PATTERNS.md` to author reviewable tournament fixtures and failing rules tests. Encode explicit `expected_order`, criterion notes, conduct-score inputs, and FIFA-ranking edition values in the fixtures so `tests/test_rules_fifa.py` can fail red on missing implementation rather than on ambiguous expectations. The tests must follow the official `03-01` finding: head-to-head criteria first, residual reapplication, global criteria, conduct score, then current and preceding FIFA ranking editions; do not create drawing-of-lots behavior. In the same task, add `tests/validators/assert_phase03_red.py` as a PowerShell-safe verifier that executes selected pytest nodes and treats the authoring task as successful only when the failures are the expected missing `cdd_mundial.simulation.rules_fifa` import, missing exported functions, or deliberate `NotImplementedError` placeholders for the rule behaviors under test. Keep the tests pure and deterministic, with no dependency on the Monte Carlo engine, and do not allow unrelated failures to count as a passing red state.</action>
  <verify>
    <automated>.\.venv\python.exe tests/validators/assert_phase03_red.py rules</automated>
  </verify>
  <done>Wave 0 pure-rules coverage exists and blocks guessed implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing slot-resolution tests from the reviewed official mapping</name>
  <files>tests/test_slot_resolution.py</files>
  <behavior>
    - Test 1: every supported best-third group set resolves to exactly one official Round-of-32 assignment.
    - Test 2: each assigned group is compatible with the corresponding fixture token family.
    - Test 3: group-position, winner, and loser slot references resolve against the frozen fixture contract.
  </behavior>
  <action>Following the contract-regression style in `tests/test_fixture.py` and the slot-integrity patterns mapped from `src/cdd_mundial/data/ingest_fixture.py`, add failing `tests/test_slot_resolution.py` cases that consume `third_place_mapping_official.json`, `third_place_expected_combinations.json`, and the real frozen fixture. The tests must prove the reviewed mapping resolves uniquely, respects the slot token family, and remains anchored to the official fixture topology per D-06. Reuse `tests/validators/assert_phase03_red.py` so the task verifies real execution failure on the missing `cdd_mundial.simulation.slots` behavior or deliberate placeholders, not just test collection. Do not implement `slots.py` here; this plan ends with red tests only.</action>
  <verify>
    <automated>.\.venv\python.exe tests/validators/assert_phase03_red.py slots</automated>
  </verify>
  <done>Wave 0 slot-resolution coverage exists and is traceable to the reviewed official mapping artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed JSON fixtures -> future rules code | Test fixtures become the authoritative expected behavior |
| reviewed mapping artifact + frozen fixture -> future slot resolver | Test expectations constrain bracket resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | rules fixtures | mitigate | Keep explicit `expected_order`, criterion notes, and fair-play inputs in reviewable JSON fixtures. |
| T-03-08 | Tampering | slot fixtures | mitigate | Consume only the reviewed official mapping artifact plus the frozen fixture contract. |
| T-03-09 | Tampering | FIFA-ranking fallback | mitigate | Pin explicit ranking-edition values in fixtures and test fallback across successive editions without randomized resolution. |
</threat_model>

<verification>
Run the Wave 0 SIM-01 red gate:

```powershell
.\.venv\python.exe tests/validators/assert_phase03_red.py all
```
</verification>

<success_criteria>
- Wave 0 SIM-01 tests exist before production rules code is written.
- The authored rules and slot nodes are executed and fail only for the intended missing `rules_fifa` / `slots` behavior before `03-04` begins.
- The official mapping gate from `03-01` is consumed, not bypassed.
- The frozen fixture slot strings remain the bracket topology authority in the test contract.
</success_criteria>

<source_audit>
GOAL
- Covered here: the rules/slot Wave 0 test harness required to implement the simulator safely.

REQ
- Covered here: `SIM-01`.
- Covered in later plans: the production SIM-01 implementation plus `SIM-02`, `SIM-03`, `SIM-04`, `SIM-05`.

RESEARCH
- Covered here: Wave 0 fixtures, token ambiguity pitfall, reviewable expected outputs.

CONTEXT
- Covered here: `D-03`, `D-04`, `D-06`.
- Supported indirectly: fair-play input semantics from `03-02`.

DEFERRED IDEAS
- No joint group outcome table is emitted.
</source_audit>

<output>
After completion, create `.planning/phases/03-simulador-del-torneo/03-03-SUMMARY.md`
</output>
