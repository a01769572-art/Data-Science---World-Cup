---
phase: 03-simulador-del-torneo
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/cdd_mundial/simulation/rules_fifa.py
  - src/cdd_mundial/simulation/slots.py
  - tests/test_rules_fifa.py
  - tests/test_slot_resolution.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Group and best-third rankings are computed by pure functions that honor the official FIFA cascade through fair play and drawing of lots (D-03, D-04)."
    - "Round-of-32 third-place slots are resolved from the reviewed official mapping while preserving the frozen fixture slot strings as topology authority (D-06)."
  artifacts:
    - path: src/cdd_mundial/simulation/rules_fifa.py
      provides: "Pure standings, mini-table, best-third, fair-play, and lot-draw functions"
    - path: src/cdd_mundial/simulation/slots.py
      provides: "Fixture-token parsing and reviewed R32 slot resolution"
  key_links:
    - from: src/cdd_mundial/simulation/rules_fifa.py
      to: tests/test_rules_fifa.py
      via: "exact expected-order fixtures and mini-table assertions"
      pattern: "test_head_to_head_subtable"
    - from: src/cdd_mundial/simulation/slots.py
      to: data/external/fixture_2026.csv
      via: "participant-slot parsing for `1A`, `3ABCDF`, `W74`, and `L101`"
      pattern: "3[A-L]"
---

<objective>
Implement the pure rules and slot-resolution layer once the official evidence, package bootstrap, and Wave 0 tests exist.

Purpose: Finish SIM-01 in a way that is deterministic, testable, and anchored to the frozen fixture topology rather than to ad hoc bracket constants.
Output: Production `rules_fifa.py` and `slots.py` passing the Wave 0 SIM-01 gates.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-simulador-del-torneo/03-CONTEXT.md
@.planning/phases/03-simulador-del-torneo/03-RESEARCH.md
@.planning/phases/03-simulador-del-torneo/03-VALIDATION.md
@.planning/phases/03-simulador-del-torneo/03-PATTERNS.md
@.planning/phases/03-simulador-del-torneo/03-02-PLAN.md
@.planning/phases/03-simulador-del-torneo/03-03-PLAN.md

<interfaces>
From `src/cdd_mundial/data/ingest_fixture.py`:

```python
def validate_fixture_structure(fixture: pd.DataFrame) -> pd.DataFrame:
def load_fixture_2026(fixture_path: Path = Path("data/external/fixture_2026.csv")) -> pd.DataFrame:
```

From `tests/test_rules_fifa.py` and `tests/test_slot_resolution.py` created in `03-03`:

```python
test_points_gd_gf_order
test_head_to_head_subtable
test_best_thirds_order
test_official_third_place_mapping_cases
test_all_official_combinations_resolve_uniquely
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fixture-token parsing and official R32 slot resolution</name>
  <files>src/cdd_mundial/simulation/slots.py, tests/test_slot_resolution.py</files>
  <behavior>
    - Test 1: group-position, winner, and loser slot references resolve against the frozen fixture without unresolved participants after their parent rounds complete.
    - Test 2: official third-place assignments resolve uniquely for every supported qualifying-group set.
    - Test 3: every assigned third-place group remains compatible with the corresponding fixture slot token family.
  </behavior>
  <action>Implement `slots.py` around the existing fixture CSV by following the exact fixture-contract import and slot-integrity patterns from `src/cdd_mundial/data/ingest_fixture.py`, as called out in `03-PATTERNS.md`. Parse `1A`, `2L`, `3ABCDF`, `W74`, and `L101` references, load the reviewed official mapping fixture produced in `03-01`, and resolve Round-of-32 participants accordingly. Keep fixture slot strings authoritative per D-06 and fail loudly on unresolved or incompatible assignments. Do not derive the mapping algorithmically from token families alone.</action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_slot_resolution.py -x</automated>
  </verify>
  <done>Third-place and winner/loser slot resolution is production-ready and traceable to the official annex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure FIFA group and best-third rules</name>
  <files>src/cdd_mundial/simulation/rules_fifa.py, tests/test_rules_fifa.py</files>
  <behavior>
    - Test 1: pure standings and tie-break functions return the exact documented order for two-way, three-way, and four-way ties.
    - Test 2: fair play and seeded drawing of lots are used only after score-based and head-to-head criteria remain tied.
    - Test 3: best-third ranking orders the twelve third-placed teams correctly and never uses cross-group head-to-head.
  </behavior>
  <action>Implement `rules_fifa.py` using the pure-transform style of `src/cdd_mundial/models/loading.py`, the NumPy-first helper style of `src/cdd_mundial/models/metrics.py`, and the seeded-lot determinism constraints captured in `03-PATTERNS.md`. The cascade must follow the official sequence pinned in `03-01`, including residual mini-table reapplication, fair play, and seeded lot draws per D-03/D-04. Keep functions deterministic for a fixed seed key and reusable by both batch simulation and direct tests. Do not load the Dixon-Coles model or Monte Carlo engine here.</action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_rules_fifa.py tests/test_slot_resolution.py -x</automated>
  </verify>
  <done>The full Phase 3 rules layer is implemented and independently testable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture slot strings -> slot resolver | Authoritative topology data drives participant resolution |
| pure match/fair-play inputs -> ranking functions | Untrusted result records become official standings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | `slots.py` | mitigate | Resolve only through fixture tokens plus reviewed official mapping; reject incompatible group assignments loudly. |
| T-03-11 | Tampering | `rules_fifa.py` | mitigate | Cover every branch with explicit fixtures for two-way, three-way, four-way, fair-play, and lot-draw ties. |
| T-03-12 | Repudiation | lot draw behavior | mitigate | Make lot draws seeded and deterministic for a fixed key, then test exchangeability when input order changes. |
</threat_model>

<verification>
Run the SIM-01 implementation gate:

```powershell
.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_rules_fifa.py tests/test_slot_resolution.py -x
```
</verification>

<success_criteria>
- `rules_fifa.py` and `slots.py` satisfy all Wave 0 SIM-01 tests.
- The official mapping gate from `03-01` is consumed, not bypassed.
- The frozen fixture slot strings remain the bracket topology authority.
</success_criteria>

<source_audit>
GOAL
- Covered here: executable pure rules and bracket resolution against official evidence.

REQ
- Covered here: `SIM-01`.
- Covered in later plans: `SIM-02`, `SIM-03`, `SIM-04`, `SIM-05`.

RESEARCH
- Covered here: rules-vs-engine separation, token ambiguity pitfall, pure-function architecture.

CONTEXT
- Covered here: `D-03`, `D-04`, `D-06`.
- Supported indirectly: fair-play input semantics from `03-02`.

DEFERRED IDEAS
- No joint group outcome table is emitted.
</source_audit>

<output>
After completion, create `.planning/phases/03-simulador-del-torneo/03-04-SUMMARY.md`
</output>
