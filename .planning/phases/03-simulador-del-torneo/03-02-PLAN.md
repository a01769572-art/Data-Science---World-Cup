---
phase: 03-simulador-del-torneo
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cdd_mundial/simulation/__init__.py
  - src/cdd_mundial/simulation/state.py
  - src/cdd_mundial/simulation/knockout.py
  - tests/fixtures/tournament/conditioned_results.json
  - tests/test_tournament_state.py
  - tests/test_knockout.py
autonomous: true
requirements: [SIM-03, SIM-05]
must_haves:
  truths:
    - "The simulation package exists and exports the first stable Phase 3 contracts before any later plan imports from it."
    - "Played tournament results are stored as thin `team_a`/`team_b` records and stay fixed across every simulation seed (D-01, D-02)."
    - "Knockout matches that are drawn after 90 minutes still produce exactly one advancing team with no measurable order bias, without introducing an extra-time lambda model (D-07, D-08)."
  artifacts:
    - path: src/cdd_mundial/simulation/__init__.py
      provides: "Bootstrap package exports for the first simulation contracts"
    - path: src/cdd_mundial/simulation/state.py
      provides: "Thin played-result state contract and validation"
    - path: src/cdd_mundial/simulation/knockout.py
      provides: "Compact post-90-minute advancement resolver and sampler"
    - path: tests/test_tournament_state.py
      provides: "Wave 0 state and conditioning invariants"
    - path: tests/test_knockout.py
      provides: "Symmetry and unbiased-advance tests for the compact resolver"
  key_links:
    - from: src/cdd_mundial/simulation/state.py
      to: tests/fixtures/tournament/conditioned_results.json
      via: "fixture-backed constructor and validation tests"
      pattern: "conditioned_results"
    - from: src/cdd_mundial/simulation/knockout.py
      to: tests/test_knockout.py
      via: "complement, half-strength, and empirical order-bias assertions"
      pattern: "test_swapping_team_order_complements_probability"
---

<objective>
Define and implement the thin tournament-state contract plus the compact knockout draw resolver that Phase 3 needs before the full engine exists.

Purpose: Lock the two behavioral boundaries most likely to drift during simulation work, and bootstrap the `simulation` package before any downstream plan imports it.
Output: Production `__init__.py`, `state.py`, `knockout.py`, and their Wave 0 tests and fixture.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-simulador-del-torneo/03-CONTEXT.md
@.planning/phases/03-simulador-del-torneo/03-RESEARCH.md
@.planning/phases/03-simulador-del-torneo/03-VALIDATION.md
@.planning/phases/03-simulador-del-torneo/03-PATTERNS.md
@.planning/phases/02-modelos-baseline/02-03-SUMMARY.md

<interfaces>
From `src/cdd_mundial/models/__init__.py`:

```python
__all__ = ["DixonColesModel", "predict_lambdas"]
```

From `src/cdd_mundial/models/dixon_coles.py`:

```python
def wdl_from_lambdas(
def predict_lambdas(team_a: str, team_b: str, ctx: dict) -> tuple[float, float]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap the simulation package and implement thin TournamentState</name>
  <files>src/cdd_mundial/simulation/__init__.py, src/cdd_mundial/simulation/state.py, tests/fixtures/tournament/conditioned_results.json, tests/test_tournament_state.py</files>
  <behavior>
    - Test 1: `TournamentState` stores only played results keyed by `match_id`, never cached standings or resolved future bracket participants.
    - Test 2: the state rejects duplicate `match_id`, unknown teams, negative goals, and fixture-participant conflicts.
    - Test 3: optional fair-play inputs are accepted as explicit observed tie-break data, not simulated card processes.
  </behavior>
  <action>Bootstrap `src/cdd_mundial/simulation/__init__.py` first using the exact package-export pattern from `src/cdd_mundial/models/__init__.py`, then implement `state.py` with the frozen dataclass pattern from `src/cdd_mundial/data/provenance.py`, the fail-loud `__post_init__` invariant style from `src/cdd_mundial/models/dixon_coles.py`, and the identity-validation approach noted in `03-PATTERNS.md`. Per D-01 and D-02, normalize only played results with `team_a` / `team_b` semantics; per the resolved research question, allow optional observed fair-play deductions as inputs for already played matches without simulating future cards. Add `conditioned_results.json` plus tests for thin-state invariants, invalid inputs, and deterministic normalization. Do not persist derived tables, bracket state, or home/away-only naming.</action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_tournament_state.py -x</automated>
  </verify>
  <done>`TournamentState` is safe for the vectorized engine to consume without stale derived state, and the `simulation` package exists for downstream imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement compact knockout draw resolution with symmetry gates</name>
  <files>src/cdd_mundial/simulation/knockout.py, tests/test_knockout.py</files>
  <behavior>
    - Test 1: swapping team order complements advancement probability exactly.
    - Test 2: identical-strength teams advance 50/50 theoretically and empirically within tolerance.
    - Test 3: every post-draw resolution produces exactly one winner without ET lambdas.
  </behavior>
  <action>Implement `knockout.py` by following the small probability-helper style of `src/cdd_mundial/models/dixon_coles.py::wdl_from_lambdas` and the invariant-heavy assertion pattern from `tests/test_metrics.py` and `tests/test_dixon_coles.py`, exactly as mapped in `03-PATTERNS.md`. Reconcile SIM-05 with D-07 and D-08 using only 90-minute `p_win`, `p_draw`, and `p_loss` inputs; do not add separate extra-time lambdas, extra-time scorelines, or order-biased tie-breaking. Document in the task implementation notes that later engine work must feed deterministic streams keyed by `match_id` plus the simulation/version seed.</action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_knockout.py -x</automated>
  </verify>
  <done>Knockout draw resolution is behaviorally complete for Phase 3 and locked against order bias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied played results -> `TournamentState` | Untrusted live-state inputs enter simulation state |
| 90-minute probabilities -> knockout winner sampler | Numeric model outputs become single-elimination decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | `TournamentState` input rows | mitigate | Reject duplicate match IDs, negative goals, unknown teams, and fixture-participant conflicts in constructor validation. |
| T-03-05 | Repudiation | seeded state conditioning | mitigate | Keep state deterministic and test played-match invariance across seeds. |
| T-03-06 | Tampering | `knockout.py` | mitigate | Enforce exact complement identities and empirical order-bias tests under fixed seeds. |
</threat_model>

<verification>
Run the Wave 0 SIM-03 and SIM-05 subset:

```powershell
.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_tournament_state.py tests/test_knockout.py -x
```
</verification>

<success_criteria>
- `TournamentState` is thin, canonical, and free of derived standings.
- Knockout draw resolution satisfies the behavioral intent of SIM-05 under D-07/D-08.
- The `simulation` package exists before later plans import from it.
</success_criteria>

<source_audit>
GOAL
- Covered here: conditional state storage and compact stochastic knockout resolution.

REQ
- Covered here: `SIM-03`, `SIM-05`.
- Covered in later plans: `SIM-01`, `SIM-02`, `SIM-04`.

RESEARCH
- Covered here: thin-state recommendation, fair-play-as-input rule, compact post-draw approximation, symmetry tests.

CONTEXT
- Covered here: `D-01`, `D-02`, `D-07`, `D-08`.
- Covered in later plans: `D-03`, `D-04`, `D-05`, `D-06`, `D-09`, `D-10`, `D-11`.

DEFERRED IDEAS
- No explicit extra-time goal submodel is introduced.
</source_audit>

<output>
After completion, create `.planning/phases/03-simulador-del-torneo/03-02-SUMMARY.md`
</output>
