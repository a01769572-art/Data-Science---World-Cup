---
phase: 03-simulador-del-torneo
plan: "05"
type: execute
wave: 4
depends_on: ["03-02", "03-04"]
files_modified:
  - pyproject.toml
  - src/cdd_mundial/simulation/engine.py
  - src/cdd_mundial/simulation/outputs.py
  - tests/test_simulation_engine.py
  - tests/test_simulation_outputs.py
  - tests/test_simulation_performance.py
  - tests/test_notebooks.py
  - notebooks/03_simulador_torneo.ipynb
autonomous: true
requirements: [SIM-02, SIM-03, SIM-04, SIM-05]
must_haves:
  truths:
    - "The simulator runs at least 10,000 complete tournaments under the hard runtime gate using a NumPy-first architecture, with fixed seeds and stable common random numbers (D-05)."
    - "Played matches remain fixed while only unresolved fixtures call `predict_lambdas(team_a, team_b, ctx)` and receive stochastic outcomes (D-01, D-02)."
    - "The outputs are stable 48-team marginal tables for advancement and group position probabilities, not joint group-state enumerations (D-09, D-10, D-11)."
    - "An executed integral notebook imports the production simulation API, explains the end-to-end workflow, and preserves a lightweight deterministic demonstration while exposing configurable 10k/100k runs (D-12 through D-15)."
  artifacts:
    - path: src/cdd_mundial/simulation/engine.py
      provides: "Vectorized tournament engine with conditioning, slot progression, and deterministic RNG control"
    - path: src/cdd_mundial/simulation/outputs.py
      provides: "Advancement and group-position probability tables"
    - path: tests/test_simulation_engine.py
      provides: "Vectorization, conditioning, reproducibility, and scalar-oracle tests"
    - path: tests/test_simulation_outputs.py
      provides: "Probability-table invariants for 48 teams and 12 groups"
    - path: tests/test_simulation_performance.py
      provides: "10k hard gate and 100k measured target benchmark"
    - path: notebooks/03_simulador_torneo.ipynb
      provides: "Executed didactic orchestration of state, rules, Monte Carlo, outputs, diagnostics, and plots"
    - path: tests/test_notebooks.py
      provides: "Phase 3 notebook structure, production-import, execution-output, and configuration gates"
  key_links:
    - from: src/cdd_mundial/simulation/engine.py
      to: src/cdd_mundial/models/dixon_coles.py
      via: "injected `predict_lambdas(team_a, team_b, ctx)` contract"
      pattern: "predict_lambdas"
    - from: src/cdd_mundial/simulation/engine.py
      to: src/cdd_mundial/simulation/rules_fifa.py
      via: "group ranking and best-third selection during batch simulation"
      pattern: "best_thirds"
    - from: src/cdd_mundial/simulation/outputs.py
      to: tests/test_simulation_outputs.py
      via: "monotone round probabilities and exact group-position totals"
      pattern: "p_champion"
    - from: notebooks/03_simulador_torneo.ipynb
      to: src/cdd_mundial/simulation
      via: "imports the public production API without redefining simulation functions"
      pattern: "from cdd_mundial.simulation"
---

<objective>
Implement the vectorized tournament engine and its output tables on top of the locked rules, state, and knockout contracts.

Purpose: Finish the executable Monte Carlo simulator that Phase 4 will consume for daily reports and published forecasts.
Output: `engine.py`, `outputs.py`, an executed integral simulation notebook, performance tests, and full Phase 3 regression gates.
</objective>

<execution_context>
@C:/Users/jesus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jesus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-simulador-del-torneo/03-CONTEXT.md
@.planning/phases/03-simulador-del-torneo/03-RESEARCH.md
@.planning/phases/03-simulador-del-torneo/03-VALIDATION.md
@.planning/phases/03-simulador-del-torneo/03-PATTERNS.md
@.planning/phases/02-modelos-baseline/02-03-SUMMARY.md
@.planning/phases/02-modelos-baseline/02-04-SUMMARY.md
@.planning/phases/03-simulador-del-torneo/03-02-PLAN.md
@.planning/phases/03-simulador-del-torneo/03-04-PLAN.md

<interfaces>
From `src/cdd_mundial/models/dixon_coles.py`:

```python
def predict_lambdas(team_a: str, team_b: str, ctx: dict) -> tuple[float, float]:
def wdl_from_lambdas(
```

From `03-02` / `03-04` outputs:

```python
TournamentState
resolve_slot(...)
rank_group(...)
rank_best_thirds(...)
sample_post_draw_winner(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the vectorized engine with conditioning and deterministic RNG streams</name>
  <files>pyproject.toml, src/cdd_mundial/simulation/engine.py, tests/test_simulation_engine.py, tests/test_simulation_performance.py</files>
  <behavior>
    - Test 1: same seed produces bit-reproducible counts and tables; different seeds change at least one unresolved match and aggregate outcome.
    - Test 2: played results remain fixed and only unresolved fixtures trigger predictor calls.
    - Test 3: still-unplayed matches keep stable random streams across state updates via deterministic `SeedSequence` partitioning keyed by `match_id` plus simulation/version seed.
    - Test 4: 10,000 complete tournament simulations finish under 60 seconds, with a separate measured 100,000 benchmark report.
  </behavior>
  <action>Implement `engine.py` as the NumPy-first tournament executor required by D-05, following the vectorized core patterns from `src/cdd_mundial/models/dixon_coles.py` and the seeded-RNG test style documented in `03-PATTERNS.md`. Convert the fixture into stable canonical ordering, overlay `TournamentState` played results, batch unresolved fixtures by stage, call the existing `predict_lambdas(team_a, team_b, ctx)` contract without renaming its semantics, sample group-stage scores vectorially, resolve standings through `rules_fifa.py`, assign third-place qualifiers through `slots.py`, and advance knockouts through `knockout.py`. Per the resolved research question, document and implement the exact `numpy.random.SeedSequence` approach using stable `match_id` plus simulation/version seed so unresolved matches preserve common-random-number comparability across state updates. Add `@pytest.mark.performance` support to `pyproject.toml` and implement the `tests/test_simulation_engine.py` and `tests/test_simulation_performance.py` gates from `03-VALIDATION.md`.</action>
  <verify>
    <automated>powershell -NoProfile -Command "& { .\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_simulation_engine.py -x -m 'not performance'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_simulation_performance.py -m performance -x; exit $LASTEXITCODE }"</automated>
  </verify>
  <done>The simulator core is ready to generate tournament counts reproducibly at the required batch scale, and the hard 10k-under-60s performance gate is part of the task-level proof for SIM-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit stable advancement and group-position marginals</name>
  <files>src/cdd_mundial/simulation/outputs.py, tests/test_simulation_outputs.py</files>
  <behavior>
    - Test 1: advancement output contains one row per canonical team with monotone round probabilities and exact tournament-total invariants.
    - Test 2: group-position output contains one row per team and satisfies row totals, per-group totals, and tournament totals exactly within tolerance.
    - Test 3: no joint group-configuration table is emitted.
  </behavior>
  <action>Implement `outputs.py` using the validated-artifact pattern from `src/cdd_mundial/data/ingest_elo.py`, the array-to-table invariant style from `src/cdd_mundial/models/metrics.py`, and the schema-minded testing style from `tests/test_contracts.py`, exactly as mapped in `03-PATTERNS.md`. Return stable per-team marginal tables for `P(R32)`, `P(R16)`, `P(QF)`, `P(SF)`, `P(Final)`, `P(Campeon)` and per-group `P(1st)`, `P(2nd)`, `P(3rd)`, `P(4th)` in line with D-09, D-10, and D-11. Keep the output layer separate from the engine loop, convert counts to probabilities deterministically, and add invariant-heavy tests rather than exact Monte Carlo expectations.</action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_simulation_outputs.py -x</automated>
  </verify>
  <done>Phase 3 emits the exact marginal outputs the roadmap requires and the full validation matrix is runnable.</done>
</task>

<task type="auto">
  <name>Task 3: Build and execute the integral Phase 3 simulation notebook</name>
  <files>notebooks/03_simulador_torneo.ipynb, tests/test_notebooks.py</files>
  <behavior>
    - Test 1: the notebook imports production functions from `cdd_mundial.simulation` and contains no function or class definitions that duplicate engine logic.
    - Test 2: every code cell follows Markdown `What and why` -> code -> Markdown `Interpretation`, uses the deterministic `python3` kernel, and contains no secret material.
    - Test 3: the committed notebook has non-empty outputs from a lightweight deterministic run and documents a visible configuration for quick, 10k, and 100k simulation counts.
    - Test 4: the notebook presents state conditioning, official rules, Monte Carlo execution, advancement/group-position tables, diagnostics, and at least one plot.
  </behavior>
  <action>Create `notebooks/03_simulador_torneo.ipynb` as the single integral didactic artifact required by D-12 through D-15. Import the public production API from `cdd_mundial.simulation`; do not redefine reusable functions, classes, rules, or engine loops in notebook cells. Organize the narrative from fixture and `TournamentState`, through official rule resolution and seeded simulation, to advancement and group-position marginals, runtime/reproducibility diagnostics, and plots. Add one explicit configuration cell with a lightweight default plus documented 10,000 and 100,000 options. Execute and commit the notebook with the lightweight deterministic outputs preserved. Extend `tests/test_notebooks.py` with Phase 3-specific gates for production imports, required analysis sections, non-empty executed outputs, configuration visibility, deterministic kernel metadata, and the existing no-redefinition contract. Do not make the notebook Run All path execute 10k or 100k by default; those remain opt-in and the hard performance proof stays in `tests/test_simulation_performance.py`. Close the phase only after the notebook gate, Phase 3 quick suite, contract regression suite, and performance gate from `03-VALIDATION.md` all pass.</action>
  <verify>
    <automated>.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_notebooks.py -x</automated>
  </verify>
  <done>The repository contains one executed, reviewable, end-to-end Phase 3 notebook backed exclusively by tested production modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture + `TournamentState` + predictor -> engine | Multiple validated inputs combine into stochastic tournament execution |
| engine counts -> published probability tables | Internal counts become user-facing forecast artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | `engine.py` fixture ordering | mitigate | Canonicalize fixture order by `match_id` and test row-order invariance. |
| T-03-14 | Repudiation | RNG streams | mitigate | Use deterministic seeded `numpy.random.Generator` streams derived from `SeedSequence` and assert bit-reproducibility plus stable CRN behavior across state updates. |
| T-03-15 | Tampering | `outputs.py` probability tables | mitigate | Enforce exact total/monotonicity invariants in `tests/test_simulation_outputs.py`. |
| T-03-16 | Repudiation | notebook analysis | mitigate | Require executed deterministic outputs, visible seed/count configuration, and imports from the tested production API. |
</threat_model>

<verification>
Run the exact Phase 3 validation commands from `03-VALIDATION.md`:

```powershell
.\.venv\python.exe -m pytest -q -p no:cacheprovider `
  tests/test_rules_fifa.py `
  tests/test_slot_resolution.py `
  tests/test_tournament_state.py `
  tests/test_simulation_engine.py `
  tests/test_simulation_outputs.py `
  tests/test_knockout.py `
  -m "not performance"
```

```powershell
.\.venv\python.exe -m pytest -q -p no:cacheprovider `
  tests/test_fixture.py `
  tests/test_dixon_coles.py `
  tests/test_rules_fifa.py `
  tests/test_slot_resolution.py `
  tests/test_tournament_state.py `
  tests/test_simulation_engine.py `
  tests/test_simulation_outputs.py `
  tests/test_knockout.py `
  -m "not performance"
```

```powershell
.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_simulation_performance.py -m performance
```

```powershell
.\.venv\python.exe -m pytest -q -p no:cacheprovider tests/test_notebooks.py
```
</verification>

<success_criteria>
- SIM-02, SIM-03, SIM-04, and SIM-05 pass their automated tests.
- The 10k runtime hard gate is encoded and green before phase completion.
- Downstream Phase 4 can import the simulation package and consume stable marginal outputs without reinterpretation.
- `notebooks/03_simulador_torneo.ipynb` is committed with deterministic quick-run outputs and passes all notebook pedagogy and hygiene gates.
</success_criteria>

<source_audit>
GOAL
- Covered here: vectorized Monte Carlo execution conditioned on real state, with stable marginal outputs.

REQ
- Covered here: `SIM-02`, `SIM-03`, `SIM-04`, `SIM-05`.
- Covered in prior plans: `SIM-01`.

RESEARCH
- Covered here: NumPy-first architecture, deterministic `SeedSequence` streams by `match_id`, output invariants, performance marker requirement.

CONTEXT
- Covered here: `D-05`, `D-09`, `D-10`, `D-11`, `D-12`, `D-13`, `D-14`, `D-15`.
- Consumes earlier decisions: `D-01`, `D-02`, `D-06`, `D-07`, `D-08`.

DEFERRED IDEAS
- No joint group-state table or explicit extra-time scoring model is emitted.
</source_audit>

<output>
After completion, create `.planning/phases/03-simulador-del-torneo/03-05-SUMMARY.md`
</output>
